---
phase: 04-moco
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/queue.py
  - core/__init__.py
  - tests/test_queue.py
autonomous: true
requirements:
  - INFRA-03

must_haves:
  truths:
    - "MomentumQueue stores L2-normalized vectors in a fixed-size FIFO buffer"
    - "After filling, queue size stays at exactly K regardless of batch size"
    - "Pointer wraps around correctly when ptr + batch_size > queue_size"
    - "get_negatives() returns detached queue contents"
  artifacts:
    - path: "core/queue.py"
      provides: "MomentumQueue FIFO buffer class"
      contains: "class MomentumQueue"
    - path: "tests/test_queue.py"
      provides: "Unit tests for MomentumQueue"
      contains: "test_queue_size_invariant"
  key_links:
    - from: "core/queue.py"
      to: "torch.nn.Module"
      via: "register_buffer for queue tensor and pointer"
      pattern: "register_buffer"
    - from: "core/__init__.py"
      to: "core/queue.py"
      via: "re-export MomentumQueue"
      pattern: "from core.queue import MomentumQueue"
---

<objective>
Implement `MomentumQueue(queue_size, dim)` — the FIFO negative-key buffer shared by MoCo v1 and v2.

Purpose: MomentumQueue is the core infrastructure component (INFRA-03) that enables MoCo's queue-based contrastive learning. It must be implemented and tested before the MoCo modules can be built.
Output: `core/queue.py` with MomentumQueue class, `tests/test_queue.py` with comprehensive unit tests, updated `core/__init__.py` re-export.
</objective>

<execution_context>
@/Users/yi-tingli/Documents/Projects/ml_topic_contrastive_learning/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yi-tingli/Documents/Projects/ml_topic_contrastive_learning/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-moco/04-CONTEXT.md
@.planning/phases/04-moco/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From core/__init__.py — re-export pattern:
```python
try:
    from core.memory_bank import MemoryBank
except ImportError:
    pass
```
MomentumQueue follows the same try/except re-export pattern.

From core/memory_bank.py — reference buffer pattern (queue uses register_buffer instead of nn.Embedding):
```python
class MemoryBank(nn.Module):
    def __init__(self, n_samples: int, dim: int) -> None:
        ...
    def get(self, indices: torch.Tensor) -> torch.Tensor:
        ...
    def update(self, indices: torch.Tensor, features: torch.Tensor) -> None:
        ...
```

From core/losses.py — queue argument interface (MomentumQueue.get_negatives() must return shape [D, K]):
```python
def _asymmetric_loss(self, z_i, z_j, queue: torch.Tensor) -> torch.Tensor:
    """queue: Negative key buffer, shape [D, K]."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MomentumQueue implementation with tests</name>
  <files>core/queue.py, tests/test_queue.py</files>
  <behavior>
    - Test: Queue initializes with L2-normalized random vectors of shape [dim, queue_size]
    - Test: After enqueuing `queue_size` vectors, queue size is exactly `queue_size` (size invariant)
    - Test: After enqueuing `queue_size + batch_size` vectors, queue size remains `queue_size`
    - Test: Pointer wrap-around: enqueue vectors when ptr=queue_size-1 with batch_size>1 correctly wraps
    - Test: get_negatives() returns tensor of shape [D, K] that is detached (requires_grad=False)
    - Test: get_negatives() returns a clone (mutating returned tensor does not affect internal queue)
    - Test: FIFO order — newest keys replace oldest keys (not newest-first)
    - Test: Queue and pointer survive state_dict save/load (register_buffer)
    - Test: update() L2-normalizes keys before storing (even if input not normalized)
  </behavior>
  <action>
    **Write tests first (RED):**
    Create `tests/test_queue.py` with tests for all behaviors listed above. Use small queue sizes (e.g., queue_size=8, dim=4) for fast tests. Use `torch.manual_seed` for reproducibility.

    Test patterns to follow (from tests/test_simclr.py):
    - Import from `core.config` for TrainConfig
    - Use `pytest.fixture` for shared setup
    - Use `torch.testing.assert_close` for tensor comparisons

    **Write implementation (GREEN):**
    Create `core/queue.py` with `MomentumQueue(nn.Module)`:
    - `__init__(self, queue_size: int, dim: int)`: Initialize queue with `torch.randn(dim, queue_size)`, L2-normalize along dim=0. Use `register_buffer("queue", ...)` and `register_buffer("ptr", torch.zeros(1, dtype=torch.long))`. Store `queue_size` and `dim` as instance attributes.
    - `update(self, keys: torch.Tensor)`: Decorated with `@torch.no_grad()`. L2-normalize keys via `F.normalize(keys.detach(), dim=1)`. Handle wrap-around: if `ptr + batch_size <= queue_size`, simple slice assignment; else split write across buffer boundary. Update pointer: `self.ptr[0] = (ptr + batch_size) % self.queue_size`.
    - `get_negatives(self) -> torch.Tensor`: Return `self.queue.clone().detach()` with shape [D, K].

    Per D-03: File is `core/queue.py`, separate from `core/memory_bank.py`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_queue.py -x -q</automated>
  </verify>
  <done>All MomentumQueue tests pass: FIFO ordering, size invariant after filling, pointer wrap-around, get_negatives detached clone, register_buffer persistence, L2-normalization on update.</done>
</task>

<task type="auto">
  <name>Task 2: Re-export MomentumQueue from core package</name>
  <files>core/__init__.py</files>
  <action>
    Add MomentumQueue to `core/__init__.py` following the existing try/except pattern used by MemoryBank:
    ```python
    try:
        from core.queue import MomentumQueue
    except ImportError:
        pass
    ```
    Add `"MomentumQueue"` to the `__all__` list.

    Place the import block after the MemoryBank block (natural ordering: memory_bank then queue).
  </action>
  <verify>
    <automated>python -c "from core import MomentumQueue; print('OK:', MomentumQueue)"</automated>
  </verify>
  <done>`from core import MomentumQueue` works and returns the MomentumQueue class.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_queue.py -x -q` — all queue unit tests pass
- `python -c "from core import MomentumQueue; q = MomentumQueue(64, 16); print(q.get_negatives().shape)"` — prints `torch.Size([16, 64])`
</verification>

<success_criteria>
- MomentumQueue class exists in core/queue.py with register_buffer for checkpoint persistence
- All FIFO, wrap-around, size-invariant, detach, and normalization tests pass
- MomentumQueue is importable from the core package
</success_criteria>

<output>
After completion, create `.planning/phases/04-moco/04-01-SUMMARY.md`
</output>
