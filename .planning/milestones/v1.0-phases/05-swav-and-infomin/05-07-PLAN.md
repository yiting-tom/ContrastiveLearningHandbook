---
phase: 05-swav-and-infomin
plan: 07
type: execute
wave: 3
depends_on:
  - 05-05
  - 05-06
files_modified:
  - configs/swav_resnet18.yaml
  - configs/infomin_resnet18.yaml
  - methods/swav/module.py
  - methods/infomin/module.py
  - tests/test_swav.py
  - tests/test_infomin.py
autonomous: true
requirements:
  - ERA2-05
  - ERA2-06
  - INFRA-04
user_setup: []

must_haves:
  truths:
    - "SwAV YAML config with 2-large + 6-small crop variant exists and validates"
    - "InfoMin YAML config exists and validates"
    - "SwAV smoke-test passes for 3 epochs from YAML"
    - "InfoMin smoke-test passes for 3 epochs from YAML"
    - "DOC-02 docstrings present on SwAVModule and InfoMinModule"
    - "Memory usage warning documented in SwAV config"
  artifacts:
    - path: "configs/swav_resnet18.yaml"
      provides: "SwAV training configuration"
      contains: "method: swav"
    - path: "configs/infomin_resnet18.yaml"
      provides: "InfoMin training configuration"
      contains: "method: infomin"
  key_links:
    - from: "configs/swav_resnet18.yaml"
      to: "core/config.py"
      via: "YAML loads into TrainConfig with SwAVConfig sub-config"
      pattern: "swav:"
    - from: "configs/infomin_resnet18.yaml"
      to: "core/config.py"
      via: "YAML loads into TrainConfig with InfoMinConfig sub-config"
      pattern: "infomin:"
---

<objective>
Write per-method YAML configs, add DOC-02 docstrings, smoke-test both methods for 3 epochs, and document memory warning.

Purpose: Final integration and documentation for Phase 5. Ensures both methods are runnable from YAML configs end-to-end with proper documentation.
Output: YAML configs, updated docstrings, smoke tests.
</objective>

<execution_context>
@/Users/yi-tingli/Documents/Projects/ml_topic_contrastive_learning/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yi-tingli/Documents/Projects/ml_topic_contrastive_learning/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-swav-and-infomin/05-CONTEXT.md
@.planning/phases/05-swav-and-infomin/05-05-SUMMARY.md
@.planning/phases/05-swav-and-infomin/05-06-SUMMARY.md

<interfaces>
From core/config.py (after Plan 04):
```python
class SwAVConfig(_StrictBase):
    n_prototypes: int = 3000
    freeze_prototypes_epochs: int = 1
    sinkhorn_iterations: int = 3
    temperature: float = 0.1
    epsilon: float = 0.05
    n_large_crops: int = 2
    large_size: int = 224
    n_small_crops: int = 6
    small_size: int = 96

class InfoMinConfig(_StrictBase):
    temperature: float = 0.5
    projection_dim: int = 128
    color_strength: float = 1.5
    grayscale_prob: float = 0.4
    use_blur: bool = False
```

From configs/moco_v1_resnet18.yaml (reference YAML pattern):
```yaml
method: moco_v1
backbone: resnet18
pretrained: false
max_epochs: 200
# ...
moco:
  temperature: 0.07
  queue_size: 65536
  momentum: 0.999
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create YAML configs and add DOC-02 docstrings</name>
  <files>configs/swav_resnet18.yaml, configs/infomin_resnet18.yaml, methods/swav/module.py, methods/infomin/module.py</files>
  <read_first>
    - configs/moco_v1_resnet18.yaml (YAML config pattern reference)
    - configs/simclr_v1_resnet18.yaml (another YAML pattern reference)
    - methods/moco/module.py (DOC-02 docstring reference — paper, authors, venue, arXiv, gotchas, reference impl)
    - methods/simclr/module.py (DOC-02 docstring reference)
    - methods/swav/module.py (current state — add docstring)
    - methods/infomin/module.py (current state — add docstring)
    - .planning/phases/05-swav-and-infomin/05-CONTEXT.md (specific docstring requirements)
    - .planning/REQUIREMENTS.md (ERA2-05 gotchas, ERA2-06 notes)
  </read_first>
  <action>
1. Create `configs/swav_resnet18.yaml`:
   ```yaml
   # SwAV (Caron et al., NeurIPS 2020) -- ResNet-18 tutorial config
   # Unsupervised Learning of Visual Features by Contrasting Cluster Assignments
   #
   # Online clustering via Sinkhorn-Knopp optimal transport.
   # Multi-crop: 2 large (224x224) + 6 small (96x96) = 8 crops per image.
   #
   # Memory usage with 8 crops is ~4x SimCLR -- reduce batch_size accordingly.
   # With ResNet-18 and 8 crops, batch_size=64 is a safe starting point.
   # For ResNet-50, reduce to batch_size=32 or use gradient accumulation.
   #
   # Prototypes are frozen during the first epoch (freeze_prototypes_epochs=1)
   # to allow the backbone to warm up before cluster assignments stabilize.

   method: swav
   backbone: resnet18
   pretrained: false

   max_epochs: 200
   warmup_epochs: 10
   batch_size: 64
   lr: 0.3
   weight_decay: 1e-6
   optimizer: lars
   scheduler: warmup_cosine
   n_views: 8
   data_dir: data
   num_workers: 4

   swav:
     n_prototypes: 3000
     freeze_prototypes_epochs: 1
     sinkhorn_iterations: 3
     temperature: 0.1
     epsilon: 0.05
     n_large_crops: 2
     large_size: 224
     n_small_crops: 6
     small_size: 96
   ```

2. Create `configs/infomin_resnet18.yaml`:
   ```yaml
   # InfoMin (Tian et al., NeurIPS 2020) -- ResNet-18 tutorial config
   # What Makes for Good Views for Contrastive Learning
   #
   # Augmentation-policy demonstration on top of SimCLR backbone.
   # Key changes from SimCLR: stronger color jitter (s=1.5 vs 1.0),
   # higher grayscale probability (0.4 vs 0.2), NO Gaussian blur.
   #
   # The InfoMin principle: views should share task-relevant information
   # but minimize mutual information beyond that. Removing blur and
   # increasing color distortion strips low-level texture correlations.
   #
   # Full semi-supervised view-learning variant deferred to v2 scope.

   method: infomin
   backbone: resnet18
   pretrained: false

   max_epochs: 200
   warmup_epochs: 10
   batch_size: 256
   lr: 0.3
   weight_decay: 1e-6
   optimizer: lars
   scheduler: warmup_cosine
   n_views: 2
   data_dir: data
   num_workers: 4

   infomin:
     temperature: 0.5
     projection_dim: 128
     color_strength: 1.5
     grayscale_prob: 0.4
     use_blur: false
   ```

3. Update `methods/swav/module.py` — replace the class docstring of `SwAVModule` with full DOC-02:
   ```
   Paper: "Unsupervised Learning of Visual Features by Contrasting Cluster Assignments"
   Authors: Mathilde Caron, Ishan Misra, Julien Mairal, Priya Goyal, Piotr Bojanowski, Armand Joulin
   Venue: NeurIPS 2020
   arXiv: https://arxiv.org/abs/2006.09882

   Algorithm:
   1. Multi-crop: produce 2 large (224x224) + N small (96x96) crops per image.
   2. Encode all crops through shared backbone + 2-layer projector.
   3. Compute prototype scores for all crops (normalized features @ prototypes).
   4. For each large crop, compute soft assignment codes via Sinkhorn-Knopp.
   5. Swapped prediction: all other crops predict each large crop's codes.
   6. Loss is cross-entropy between predictions and codes, averaged over pairs.

   Gotchas:
   - Prototypes must be frozen for the first `freeze_prototypes_epochs` epochs
     (default: 1). Training without this causes unstable cluster assignments.
   - Prototype vectors must be L2-normalized after every optimizer step. Without
     this, score magnitudes grow unboundedly and Sinkhorn-Knopp overflows.
   - Sinkhorn-Knopp code matrix must be doubly stochastic -- each prototype
     gets equal assignment mass. Use epsilon=0.05 (or 0.03 if unstable).
   - Memory usage with 8 crops is ~4x SimCLR. Reduce batch_size to 1/4 of
     SimCLR batch size (e.g., 64 vs 256 for ResNet-18).

   Reference implementation: https://github.com/facebookresearch/swav
   ```

4. Update `methods/infomin/module.py` — replace the class docstring of `InfoMinModule` with full DOC-02:
   ```
   Paper: "What Makes for Good Views for Contrastive Learning?"
   Authors: Yonglong Tian, Chen Sun, Ben Poole, Dilip Krishnan, Cordelia Schmid, Phillip Isola
   Venue: NeurIPS 2020
   arXiv: https://arxiv.org/abs/2005.10243

   Algorithm:
   1. Use SimCLR backbone and NT-Xent loss unchanged.
   2. Replace augmentation with aggressive "minimal MI" policy:
      - Color jitter s=1.5 (vs SimCLR's s=1.0)
      - Random grayscale p=0.4 (vs SimCLR's p=0.2)
      - NO Gaussian blur (removed entirely)
   3. The InfoMin principle: views should share task-relevant information
      but minimize mutual information beyond that. More aggressive
      augmentation removes spurious correlations (texture, color bias).

   Gotchas:
   - This implementation is the augmentation-policy interpretation of InfoMin,
     not the full semi-supervised view-learning framework (deferred to v2).
   - The augmentation override must be applied at data setup time, not just
     at module construction. Use build_augmentation() to get the transform.
   - InfoMin augmentation may need batch_size tuning compared to SimCLR
     because harder augmentations can slow convergence.

   Reference implementation: https://github.com/HobbitLong/PyContrast
   ```
  </action>
  <verify>
    <automated>cd /Users/yi-tingli/Documents/Projects/ml_topic_contrastive_learning && python -c "from core.config import load_config; cfg = load_config('configs/swav_resnet18.yaml'); print(f'SwAV OK: {cfg.swav.n_large_crops} large, {cfg.swav.n_small_crops} small'); cfg2 = load_config('configs/infomin_resnet18.yaml'); print(f'InfoMin OK: {cfg2.infomin.color_strength}')"</automated>
  </verify>
  <acceptance_criteria>
    - configs/swav_resnet18.yaml contains `method: swav`
    - configs/swav_resnet18.yaml contains `# Memory usage with 8 crops`
    - configs/swav_resnet18.yaml contains `n_large_crops: 2`
    - configs/swav_resnet18.yaml contains `n_small_crops: 6`
    - configs/swav_resnet18.yaml contains `optimizer: lars`
    - configs/infomin_resnet18.yaml contains `method: infomin`
    - configs/infomin_resnet18.yaml contains `color_strength: 1.5`
    - configs/infomin_resnet18.yaml contains `use_blur: false`
    - methods/swav/module.py docstring contains `arXiv: https://arxiv.org/abs/2006.09882`
    - methods/swav/module.py docstring contains `Gotchas:`
    - methods/infomin/module.py docstring contains `arXiv: https://arxiv.org/abs/2005.10243`
    - methods/infomin/module.py docstring contains `Gotchas:`
    - Both YAML configs validate via `load_config()` without ValidationError
  </acceptance_criteria>
  <done>YAML configs validate, DOC-02 docstrings include paper/authors/venue/arXiv/gotchas/reference for both methods</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-test both methods for 3 epochs from YAML configs</name>
  <files>tests/test_swav.py, tests/test_infomin.py</files>
  <read_first>
    - tests/test_moco.py (reference for end-to-end YAML smoke test pattern — look for config loading + training tests)
    - tests/test_simclr.py (reference for smoke test with synthetic data and loss check)
    - configs/swav_resnet18.yaml (SwAV config to validate)
    - configs/infomin_resnet18.yaml (InfoMin config to validate)
    - tests/test_swav.py (current state — append smoke tests)
    - tests/test_infomin.py (current state — append smoke tests)
  </read_first>
  <action>
Add smoke tests to existing test files:

1. In `tests/test_swav.py`, add:
   - `test_swav_yaml_config_validates()`: load `configs/swav_resnet18.yaml` via `load_config()`, assert `cfg.method == "swav"`, assert `cfg.swav.n_large_crops == 2`, assert `cfg.swav.n_small_crops == 6`
   - `test_swav_smoke_3_epochs(large_imagefolder)`: Load SwAV config but override `max_epochs=3, backbone=resnet18, batch_size=8, warmup_epochs=0`, set `n_large_crops=2, n_small_crops=2, large_size=32, small_size=16, n_prototypes=10` for speed. Create `MultiCropDataset` wrapping ImageFolder, pass to `SSLDataModule(data_dir=..., dataset=multi_crop_ds, batch_size=8)`. Train `SwAVModule` with `L.Trainer(max_epochs=3, enable_checkpointing=False, logger=False, accelerator="cpu", devices=1)`. Assert no NaN loss. Assert prototype norms ≈ 1.0 after training.

2. In `tests/test_infomin.py`, add:
   - `test_infomin_yaml_config_validates()`: load `configs/infomin_resnet18.yaml` via `load_config()`, assert `cfg.method == "infomin"`, assert `cfg.infomin.color_strength == 1.5`, assert `cfg.infomin.use_blur == False`
   - `test_infomin_smoke_3_epochs(large_imagefolder)`: Create InfoMin augmentation via `InfoMinModule.build_augmentation(size=32)`, wrap in `MultiViewTransform(aug, n_views=2)`, create ImageFolder with transform, wrap in `SSLDataModule(data_dir=..., batch_size=8)`. Train `InfoMinModule` for 3 epochs with same Trainer settings. Assert no NaN loss.

Both smoke tests should use the `large_imagefolder` fixture (3 classes, 40 images, 32x32 pixels) and the `clean_registry` fixture.
  </action>
  <verify>
    <automated>cd /Users/yi-tingli/Documents/Projects/ml_topic_contrastive_learning && python -m pytest tests/test_swav.py tests/test_infomin.py -x -v -k "smoke or yaml_config"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_swav.py contains `test_swav_yaml_config_validates`
    - tests/test_swav.py contains `test_swav_smoke_3_epochs`
    - tests/test_infomin.py contains `test_infomin_yaml_config_validates`
    - tests/test_infomin.py contains `test_infomin_smoke_3_epochs`
    - `python -m pytest tests/test_swav.py tests/test_infomin.py -x -v -k "smoke or yaml_config"` exits 0
    - `python -m pytest tests/ -x` exits 0 (full regression)
  </acceptance_criteria>
  <done>Both methods smoke-test for 3 epochs without errors, YAML configs validate, full test suite passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML config files | User-authored training configurations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | T | YAML config | mitigate | Pydantic extra='forbid' validates all fields; type checking on all values |
</threat_model>

<verification>
- `python -m pytest tests/test_swav.py tests/test_infomin.py -x -v` — all tests pass
- `python -m pytest tests/ -x` — full suite green, no regression
- Both YAML configs load without error
- DOC-02 docstrings present on both modules
</verification>

<success_criteria>
SwAV and InfoMin YAML configs validate and are documented with memory warnings. Both methods pass 3-epoch smoke tests. DOC-02 docstrings include all required fields (paper, authors, venue, arXiv, gotchas, reference impl).
</success_criteria>

<output>
After completion, create `.planning/phases/05-swav-and-infomin/05-07-SUMMARY.md`
</output>
