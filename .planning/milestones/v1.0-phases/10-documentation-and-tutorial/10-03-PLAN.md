---
phase: 10
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - docs/tutorial/01_add_a_new_method.md
autonomous: true
requirements:
  - DOC-03
must_haves:
  truths:
    - "A reader who follows section (a) end-to-end can add a working SSL method to the repo without referring to other docs"
    - "Section (a) walks through subclassing BaseSSLModule, implementing build_projector and training_step, registering in method_dispatcher, and writing a YAML config"
    - "Every code snippet uses real imports and class names from the repo (not pseudocode)"
    - "Section (a) ends with a verification command (python train.py --config configs/<newmethod>.yaml --data-dir <toy>)"
  artifacts:
    - path: "docs/tutorial/01_add_a_new_method.md"
      provides: "DOC-03 section (a) — step-by-step guide for adding a new SSL method"
      contains: "BaseSSLModule"
      min_lines: 150
  key_links:
    - from: "docs/tutorial/01_add_a_new_method.md"
      to: "core.base.BaseSSLModule"
      via: "code snippet showing subclass"
      pattern: "class \\w+\\(BaseSSLModule\\)"
    - from: "docs/tutorial/01_add_a_new_method.md"
      to: "core.dispatcher.register_method"
      via: "code snippet showing registration"
      pattern: "register_method"
    - from: "docs/tutorial/01_add_a_new_method.md"
      to: "train.py"
      via: "documented run command"
      pattern: "python train\\.py --config"
---

<objective>
Write tutorial section (a) of the DOC-03 walkthrough: "How to add a new method".

This is one of three sibling tutorial sections (sections (a), (b), (c) — Plans 10-03, 10-04,
10-05) that get assembled into the final `docs/tutorial.md` by Plan 10-06. Each section is
written as a standalone Markdown file under `docs/tutorial/` so they can be drafted in
parallel and combined later.

Purpose: A reader who has cloned the repo and read README.md should, after reading section
(a), be able to implement a NEW SSL method (or copy an existing one) end-to-end —
LightningModule subclass, dispatcher registration, YAML config — and run it via train.py.

Output: `docs/tutorial/01_add_a_new_method.md` — concrete walkthrough with real code from
the repo, ending in a working "MyToyContrastive" example that registers under the key
`my_toy_contrastive` and trains on the toy ImageFolder fixture.
</objective>

<execution_context>
@/Users/yi-tingli/Documents/Projects/ml_topic_contrastive_learning/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yi-tingli/Documents/Projects/ml_topic_contrastive_learning/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/10-documentation-and-tutorial/10-RESEARCH.md
@.planning/phases/10-documentation-and-tutorial/10-PATTERNS.md
@core/base.py
@core/dispatcher.py
@core/config.py
@core/projection.py
@core/losses.py
@core/backbone.py
@methods/__init__.py
@methods/simclr/__init__.py
@methods/simclr/module.py
@methods/invariant_spread/module.py
@configs/simclr_v1_resnet18.yaml
@configs/example.yaml

<interfaces>
Real imports the tutorial code snippets must use (verified from codebase):

```python
from core.config import TrainConfig
from core.base import BaseSSLModule
from core.dispatcher import register_method, available_methods, method_dispatcher
from core.backbone import build_backbone
from core.projection import ProjectionHead
from core.losses import InfoNCELoss
```

BaseSSLModule abstract API (from core/base.py — what subclasses must override):
- build_projector(self) -> nn.Module : abstract
- training_step(self, batch, batch_idx) -> torch.Tensor : abstract
- learnable_params property defaults to self.parameters()
- configure_optimizers() handles AdamW / SGD / LARS dispatch
- on_train_batch_end() runs EMA hooks (no-op by default)
- log_train_metrics(loss) logs train/loss + train/lr

Dispatcher API (from core/dispatcher.py):
- register_method(name: str, cls: type[BaseSSLModule]) -> None
- method_dispatcher(cfg: TrainConfig) -> BaseSSLModule
- available_methods() -> list[str]
- Registration triggers via methods/__init__.py side-effect imports
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/tutorial/01_add_a_new_method.md</name>
  <files>docs/tutorial/01_add_a_new_method.md</files>
  <read_first>
    - .planning/phases/10-documentation-and-tutorial/10-PATTERNS.md (File 4: tutorial section (a) — verified imports + BaseSSLModule example)
    - .planning/phases/10-documentation-and-tutorial/10-RESEARCH.md (Adding a New Method — 6-step workflow)
    - core/base.py (full BaseSSLModule definition — copy method signatures into the tutorial)
    - core/dispatcher.py (register_method signature + module docstring example)
    - methods/invariant_spread/module.py (simplest complete example to mirror for MyToyContrastive)
    - methods/simclr/module.py (canonical 2-view InfoNCE pattern)
    - methods/__init__.py (the import-side-effect pattern for triggering registration)
    - configs/simclr_v1_resnet18.yaml (canonical YAML format)
    - core/config.py (TrainConfig field list)
  </read_first>
  <action>
Create the directory `docs/tutorial/` if it does not exist. Then write
`docs/tutorial/01_add_a_new_method.md` with the EXACT content below. The Markdown body
starts at the `# Tutorial Section (a)` heading and ends at the bottom of the file. All
code blocks must use real imports and class names from the repo — no placeholders.

Markdown content to write verbatim into `docs/tutorial/01_add_a_new_method.md`:

----- BEGIN FILE CONTENT -----
# Tutorial Section (a): How to Add a New Method

This section walks through adding a new self-supervised contrastive method to the
repository, using a small "toy contrastive" example as the worked sample. By the
end you will have:

1. A new `LightningModule` subclass under `methods/<newmethod>/`.
2. The method registered in `core.dispatcher` so `method: <key>` works in any YAML.
3. A YAML config in `configs/<newmethod>_resnet18.yaml`.
4. A successful `python train.py --config configs/<newmethod>_resnet18.yaml` run.

The whole loop is six small steps. The goal is for you to be able to copy any
existing method (e.g., `methods/invariant_spread/`) as a starting point and swap
in your own loss in under 30 lines.

## The interface: what BaseSSLModule expects

Every method in this repo subclasses `core.base.BaseSSLModule`, which is a thin
wrapper around `lightning.LightningModule` that handles the parts that are the
same for every contrastive method:

- `configure_optimizers()` dispatches AdamW / SGD / LARS based on `cfg.optimizer`,
  and attaches a warmup-cosine LR scheduler.
- `on_train_batch_end()` calls EMA updates on any registered momentum encoders
  (no-op for methods without an EMA target).
- `log_train_metrics(loss)` logs `train/loss`, `train/lr`, and per-method
  diagnostics via `self.log(...)`.
- `learnable_params` defaults to `self.parameters()`; override it to exclude EMA
  target params from the optimizer (see `methods/byol/module.py`).

A subclass MUST override two methods:

| Method | Purpose |
|--------|---------|
| `build_projector(self) -> nn.Module` | Construct the projection head (often `core.projection.ProjectionHead`) given `self.feat_dim` |
| `training_step(self, batch, batch_idx) -> torch.Tensor` | Compute the contrastive loss for one mini-batch |

Everything else (optimizer choice, augmentations, data loading) comes from the
YAML config plus the shared `SSLDataModule`.

## Step 1: Create the package directory

Each method lives in its own sub-package under `methods/`. The convention is:

```
methods/
  my_toy_contrastive/
    __init__.py    # registers the method with the dispatcher
    module.py      # the LightningModule subclass
```

```bash
mkdir methods/my_toy_contrastive
touch methods/my_toy_contrastive/__init__.py methods/my_toy_contrastive/module.py
```

## Step 2: Implement the LightningModule subclass

The minimal subclass that works with the existing infrastructure looks like this.
Copy it into `methods/my_toy_contrastive/module.py`:

```python
"""My toy contrastive method — a minimal SimCLR-like example for the tutorial.

Paper: (this is a tutorial example — no paper)
Authors: you
Venue: tutorial
arXiv: n/a

Algorithm:
1. Augment each image into 2 views.
2. Encode with shared backbone, project via 2-layer MLP.
3. Compute symmetric InfoNCE loss with temperature 0.5.

Gotchas:
- This is the smallest possible BaseSSLModule subclass. It uses InfoNCELoss
  in symmetric mode (the same path SimCLR uses).

Reference implementation: this file.
"""
from __future__ import annotations

import torch.nn as nn

from core.backbone import build_backbone
from core.base import BaseSSLModule
from core.config import TrainConfig
from core.losses import InfoNCELoss
from core.projection import ProjectionHead


class MyToyContrastiveModule(BaseSSLModule):
    """Minimal contrastive method for the tutorial."""

    def __init__(self, cfg: TrainConfig) -> None:
        super().__init__(cfg)
        self.backbone, self.feat_dim = build_backbone(cfg.backbone, cfg.pretrained)
        self.projector = self.build_projector()
        self.loss_fn = InfoNCELoss(temperature=0.5)

    def build_projector(self) -> nn.Module:
        return ProjectionHead(
            input_dim=self.feat_dim,
            hidden_dim=2048,
            output_dim=128,
            num_layers=2,
        )

    def training_step(self, batch, batch_idx):
        views, _ = batch                       # SSLDataModule yields (views, labels)
        z_i = self.projector(self.backbone(views[0]))
        z_j = self.projector(self.backbone(views[1]))
        loss = self.loss_fn(z_i, z_j)          # InfoNCELoss L2-normalizes internally
        self.log_train_metrics(loss)
        return loss
```

Things to notice:

- **Always use `backbone.num_features`** via `build_backbone(...)` — never hard-code
  2048 or 512. timm exposes `num_features` consistently across ResNets and ViTs.
- **`InfoNCELoss` L2-normalizes inputs internally**. Do not pre-normalize.
- **`views, _ = batch`** — `SSLDataModule` yields a list of view tensors plus class
  labels (the labels are unused for SSL methods; SupCon is the exception).
- **`log_train_metrics`** logs `train/loss` and `train/lr` for free; you can add
  method-specific scalars by calling `self.log("train/<name>", value)` directly
  after `log_train_metrics(loss)`.

## Step 3: Register with the dispatcher

Methods become discoverable to `core.dispatcher.method_dispatcher` via a side-effect
import. Add the registration call in `methods/my_toy_contrastive/__init__.py`:

```python
"""my_toy_contrastive package — registers MyToyContrastiveModule with the dispatcher."""
from core.dispatcher import register_method
from methods.my_toy_contrastive.module import MyToyContrastiveModule

register_method("my_toy_contrastive", MyToyContrastiveModule)
```

Then add ONE line to the top-level `methods/__init__.py` so the side-effect import
fires when `train.py` runs `import methods`:

```python
# methods/__init__.py — add this line at the bottom:
import methods.my_toy_contrastive  # noqa: F401
```

Verify the registration worked:

```bash
python -c "import methods; from core.dispatcher import available_methods; print('my_toy_contrastive' in available_methods())"
# expected: True
```

## Step 4: Write a YAML config

Configs live in `configs/`. Copy `configs/simclr_v1_resnet18.yaml` as a starting
point and rename the method key:

```yaml
# configs/my_toy_contrastive_resnet18.yaml
method: my_toy_contrastive
backbone: resnet18
pretrained: false

max_epochs: 200
warmup_epochs: 10
batch_size: 256
lr: 1e-3
weight_decay: 1e-6
optimizer: adamw
n_views: 2
data_dir: data
num_workers: 4
```

A few rules the config schema enforces (`extra='forbid'` on `TrainConfig`):

- **Unknown top-level keys raise ValidationError.** If you misspell `optimizer` as
  `optimiser`, `load_config` raises `pydantic.ValidationError` immediately — by design.
- **Per-method sub-configs are namespaced.** SimCLR puts its temperature under
  `simclr.temperature`; if your method needs config knobs, add a `MyToyContrastiveConfig`
  class to `core/config.py` (mirror `SimCLRConfig` / `MoCoConfig`).
- **Optimizer must be one of `adamw`, `sgd`, `lars`**; the value is dispatched in
  `BaseSSLModule.configure_optimizers`.

## Step 5: Run training

Train your new method exactly the same way you would train any of the 14 built-in
methods:

```bash
python train.py --config configs/my_toy_contrastive_resnet18.yaml --data-dir data/
```

For a quick sanity check, you can override `data_dir` to a small ImageFolder
fixture (or use the toy fixture from `tests/conftest.py::tmp_imagefolder`).

## Step 6: Add a smoke test (recommended)

Drop a minimal test in `tests/test_my_toy_contrastive.py`:

```python
from __future__ import annotations

import lightning as L

from core.config import TrainConfig
from core.data import SSLDataModule
from core.dispatcher import method_dispatcher


def test_my_toy_contrastive_smoke(tmp_imagefolder, toy_config_dict):
    cfg_raw = dict(toy_config_dict)
    cfg_raw["method"] = "my_toy_contrastive"
    cfg_raw["data_dir"] = str(tmp_imagefolder)
    cfg = TrainConfig.model_validate(cfg_raw)

    model = method_dispatcher(cfg)
    dm = SSLDataModule(
        data_dir=cfg.data_dir,
        n_views=cfg.n_views,
        batch_size=4,
        num_workers=0,
        size=32,
        strong=True,
    )
    trainer = L.Trainer(
        max_epochs=1, limit_train_batches=1,
        accelerator="cpu", logger=False,
        enable_checkpointing=False, enable_progress_bar=False,
    )
    trainer.fit(model, dm)
```

Run it:

```bash
pytest tests/test_my_toy_contrastive.py -x -q
```

## Where to go next

- To add a method that uses an **EMA momentum encoder** (BYOL / MoCo / DINO style),
  study `methods/byol/module.py`. It overrides `learnable_params` to exclude EMA
  target params and wires the EMA update through `on_train_batch_end`.
- To add a method that uses a **memory bank or queue** (Instance Discrimination,
  MoCo), see `core/queue.py` (`MomentumQueue`) and
  `methods/instance_discrimination/module.py` (`MemoryBank`).
- To add a **multi-crop method** (SwAV, DINO), set `n_views > 2` in the YAML;
  `SSLDataModule` automatically switches to `MultiCropDataset`.

Section (b) — *Running an experiment end-to-end* — picks up from here and walks
through training one of the 14 built-in methods, evaluating it via the `eval/`
suite, and reading the results.
----- END FILE CONTENT -----

Implementation notes:
- The directory `docs/tutorial/` does not yet exist — create it explicitly via
  `mkdir -p docs/tutorial` before writing the file.
- All imports in code blocks MUST match the real codebase (already verified above).
- Do NOT include any references to `notebooks/walkthrough.ipynb` — Phase 10 uses
  Markdown per RESEARCH.md recommendation.
- Do NOT add a "future work" or "limitations" section — out of DOC-03 scope.
- The `MyToyContrastiveModule` example exists ONLY as text in this Markdown file.
  It is NOT actually added to `methods/` — this is a tutorial example, not a real
  method. (If a future plan wants to add it as a working test fixture, that is a
  separate task.)
- Do NOT prefix Markdown content with leading spaces (they are part of the file body).
  Write the document verbatim from "# Tutorial Section (a)..." onward.
  </action>
  <verify>
    <automated>test -f docs/tutorial/01_add_a_new_method.md && grep -q "BaseSSLModule" docs/tutorial/01_add_a_new_method.md && grep -q "register_method" docs/tutorial/01_add_a_new_method.md && grep -q "build_projector" docs/tutorial/01_add_a_new_method.md && grep -q "training_step" docs/tutorial/01_add_a_new_method.md && grep -q "method_dispatcher" docs/tutorial/01_add_a_new_method.md && grep -q "python train.py --config" docs/tutorial/01_add_a_new_method.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f docs/tutorial/01_add_a_new_method.md`
    - Contains `# Tutorial Section (a): How to Add a New Method` heading: `grep -F "# Tutorial Section (a): How to Add a New Method" docs/tutorial/01_add_a_new_method.md`
    - Has all 6 step headings: each of `## Step 1:`, `## Step 2:`, `## Step 3:`, `## Step 4:`, `## Step 5:`, `## Step 6:` present: `for s in 1 2 3 4 5 6; do grep -q "## Step $s:" docs/tutorial/01_add_a_new_method.md || exit 1; done`
    - Has section "The interface: what BaseSSLModule expects": `grep -F "## The interface: what BaseSSLModule expects" docs/tutorial/01_add_a_new_method.md`
    - Has a class definition `class MyToyContrastiveModule(BaseSSLModule)`: `grep -F "class MyToyContrastiveModule(BaseSSLModule)" docs/tutorial/01_add_a_new_method.md`
    - Contains the exact `register_method("my_toy_contrastive", MyToyContrastiveModule)` line: `grep -F 'register_method("my_toy_contrastive", MyToyContrastiveModule)' docs/tutorial/01_add_a_new_method.md`
    - Contains `from core.base import BaseSSLModule`: `grep -F "from core.base import BaseSSLModule" docs/tutorial/01_add_a_new_method.md`
    - Contains `from core.backbone import build_backbone`: `grep -F "from core.backbone import build_backbone" docs/tutorial/01_add_a_new_method.md`
    - Contains `from core.projection import ProjectionHead`: `grep -F "from core.projection import ProjectionHead" docs/tutorial/01_add_a_new_method.md`
    - Contains `from core.losses import InfoNCELoss`: `grep -F "from core.losses import InfoNCELoss" docs/tutorial/01_add_a_new_method.md`
    - Contains the YAML method line: `grep -F "method: my_toy_contrastive" docs/tutorial/01_add_a_new_method.md`
    - Contains `python train.py --config configs/my_toy_contrastive_resnet18.yaml`: `grep -F "python train.py --config configs/my_toy_contrastive_resnet18.yaml" docs/tutorial/01_add_a_new_method.md`
    - File size at least 5KB: `[ $(wc -c < docs/tutorial/01_add_a_new_method.md) -ge 5000 ]`
    - No regressions: `pytest tests/ -q --tb=line` returns exit 0
  </acceptance_criteria>
  <done>
    A reader who has README.md and `docs/tutorial/01_add_a_new_method.md` open can copy
    `methods/invariant_spread/` as a template, swap in their own loss, register it under
    a new key, write a config, and run `python train.py --config ...` successfully —
    without consulting any other doc, source file, or external resource.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tutorial reader → repo code | Reader copies/adapts code snippets into their own forks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering | Outdated tutorial diverges from real API | mitigate | All code snippets reference real imports verified by Plan 10-01's executor; Plan 10-06 final review pass re-verifies |
| T-10-07 | Information Disclosure | Tutorial reveals internals that should be private | accept | All referenced classes are intentionally part of the public extension API (BaseSSLModule, ProjectionHead, InfoNCELoss, register_method) |
</threat_model>

<verification>
- File exists at `docs/tutorial/01_add_a_new_method.md`
- All acceptance_criteria grep checks pass
- Reader workflow validation (manual): a fresh reader can follow steps 1-6 and end up with a working method
- No regressions: `pytest tests/ -q` returns exit 0 (this plan modifies docs only — no code changes)
</verification>

<success_criteria>
- DOC-03 section (a) satisfied: `docs/tutorial/01_add_a_new_method.md` demonstrates adding a new method by subclassing `BaseSSLModule`
- Reader can follow the 6 steps end-to-end without referring to other docs
- All code snippets use real imports from the repo
- Plan 10-06 has the section (a) source ready for assembly into final `docs/tutorial.md`
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation-and-tutorial/10-03-SUMMARY.md`
</output>
