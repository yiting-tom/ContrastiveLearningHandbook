---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - pyproject.toml
  - core/__init__.py
  - core/config.py
  - methods/__init__.py
  - configs/example.yaml
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_config.py
autonomous: true
requirements: [FOUND-02, FOUND-08]

must_haves:
  truths:
    - "TrainConfig.model_validate(yaml.safe_load(valid_yaml)) returns a typed config object"
    - "Unknown YAML keys raise Pydantic ValidationError"
    - "EvalConfig sub-schema with LinearProbeConfig, KNNConfig, TSNEConfig, UMAPConfig, FinetuneConfig, CAMConfig is accessible under eval key"
    - "Per-method sub-configs (e.g., simclr.temperature) are Optional and validate when present"
  artifacts:
    - path: "core/config.py"
      provides: "TrainConfig, EvalConfig, all sub-configs, load_config helper"
      contains: "class TrainConfig"
    - path: "requirements.txt"
      provides: "Pinned dependency versions"
      contains: "torch==2.11.0"
    - path: "tests/conftest.py"
      provides: "Shared pytest fixtures"
      contains: "def tmp_imagefolder"
    - path: "tests/test_config.py"
      provides: "Config validation tests"
      contains: "def test_valid_config"
  key_links:
    - from: "core/config.py"
      to: "configs/example.yaml"
      via: "load_config reads YAML and validates via TrainConfig.model_validate"
      pattern: "model_validate"
---

<objective>
Set up the project scaffold (requirements.txt, pyproject.toml, package init files, shared test fixtures) and implement the Pydantic v2 config schema with all sub-configs.

Purpose: Every subsequent plan imports from core/ and runs tests — this plan creates the skeleton they depend on, plus the config system that BaseSSLModule and all methods read from.
Output: requirements.txt, pyproject.toml, core/config.py, configs/example.yaml, tests/conftest.py, all package __init__.py files
</objective>

<execution_context>
@/Users/yi-tingli/Documents/Projects/ml_topic_contrastive_learning/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yi-tingli/Documents/Projects/ml_topic_contrastive_learning/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project scaffold — requirements.txt, pyproject.toml, package inits, test fixtures</name>
  <files>requirements.txt, pyproject.toml, core/__init__.py, methods/__init__.py, tests/__init__.py, tests/conftest.py</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (standard stack versions)
    .planning/phases/01-foundation/01-CONTEXT.md (D-03 project layout, D-06/D-07 test strategy)
    .planning/phases/01-foundation/01-VALIDATION.md (Wave 0 requirements)
  </read_first>
  <action>
1. Create `requirements.txt` with pinned versions (per D-04, no lightly dependency):
   ```
   torch==2.11.0
   torchvision==0.26.0
   lightning==2.6.1
   timm==1.0.26
   pydantic==2.12.5
   pyyaml==6.0.3
   pytest==8.4.1
   ```

2. Create `pyproject.toml` with pytest config:
   ```toml
   [tool.pytest.ini_options]
   testpaths = ["tests"]
   addopts = "-x -q"
   ```

3. Create `core/__init__.py` — empty for now (will add re-exports as modules are created).

4. Create `methods/__init__.py` — empty.

5. Create `tests/__init__.py` — empty.

6. Create `tests/conftest.py` with shared fixtures (per D-06, D-07):
   - `@pytest.fixture` `random_tensor(shape)` — returns `torch.randn(*shape)`
   - `@pytest.fixture` `tmp_imagefolder(tmp_path)` — creates a temporary ImageFolder with 3 classes, 5 images each (32x32 RGB JPEGs), returns the `tmp_path` directory. Uses `PIL.Image.fromarray(np.random.randint(0, 255, (32, 32, 3), dtype=np.uint8))` to create dummy images. Directory structure: `tmp_path/class_0/img_00.jpg`, etc.
   - `@pytest.fixture` `toy_config_dict()` — returns a minimal valid config dict:
     ```python
     {
         "method": "simclr_v1",
         "backbone": "resnet18",
         "pretrained": False,
         "max_epochs": 10,
         "warmup_epochs": 1,
         "batch_size": 32,
         "lr": 0.3,
         "weight_decay": 1e-6,
         "optimizer": "adamw",
     }
     ```
  </action>
  <verify>
    <automated>python -c "import core; import methods; import tests; print('packages OK')" && pytest tests/ -x -q --co 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt contains exactly: torch==2.11.0, torchvision==0.26.0, lightning==2.6.1, timm==1.0.26, pydantic==2.12.5, pyyaml==6.0.3, pytest==8.4.1
    - pyproject.toml contains [tool.pytest.ini_options] with testpaths = ["tests"]
    - core/__init__.py exists
    - methods/__init__.py exists
    - tests/__init__.py exists
    - tests/conftest.py contains `def tmp_imagefolder` and `def toy_config_dict`
  </acceptance_criteria>
  <done>All package directories importable, requirements.txt has pinned versions, conftest.py has shared fixtures</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Pydantic v2 config schema with TrainConfig, EvalConfig, and sub-configs</name>
  <files>core/config.py, configs/example.yaml, tests/test_config.py</files>
  <read_first>
    .planning/REQUIREMENTS.md (FOUND-02 and FOUND-08 full specs)
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3: Pydantic config, Pitfall 2: extra='forbid' propagation)
    .planning/phases/01-foundation/01-CONTEXT.md (D-08: extra='forbid' on all sub-configs)
  </read_first>
  <behavior>
    - Test 1: `TrainConfig.model_validate(valid_dict)` succeeds and all fields are correctly typed
    - Test 2: `TrainConfig.model_validate(dict_with_unknown_key)` raises `ValidationError` (per D-08)
    - Test 3: Nested sub-config with unknown key raises `ValidationError` (extra='forbid' propagates)
    - Test 4: `load_config("configs/example.yaml")` returns a valid `TrainConfig` instance
    - Test 5: `EvalConfig` with `LinearProbeConfig`, `KNNConfig`, `TSNEConfig`, `UMAPConfig`, `FinetuneConfig`, `CAMConfig` sub-schemas validates correctly
    - Test 6: Per-method sub-configs (SimCLRConfig, MoCoConfig, etc.) are Optional and default to None
    - Test 7: Invalid types (e.g., lr="not_a_float") raise `ValidationError`
  </behavior>
  <action>
1. Create `core/config.py`:
   - Define `_StrictBase(BaseModel)` with `model_config = ConfigDict(extra="forbid")` — ALL config classes inherit from this (per D-08).
   - Per-method sub-configs (all inherit `_StrictBase`):
     - `SimCLRConfig`: `temperature: float = 0.5`, `projection_dim: int = 128`
     - `MoCoConfig`: `temperature: float = 0.07`, `queue_size: int = 65536`, `momentum: float = 0.999`
     - `BYOLConfig`: `base_momentum: float = 0.996`, `end_momentum: float = 1.0`
     - `SwAVConfig`: `n_prototypes: int = 3000`, `freeze_prototypes_epochs: int = 1`, `sinkhorn_iterations: int = 3`
     - `BarlowTwinsConfig`: `lambda_coeff: float = 5e-3`, `projection_dim: int = 8192`
     - `SimSiamConfig`: `predictor_hidden_dim: int = 512`
     - `DINOConfig`: `n_prototypes: int = 65536`, `teacher_temp: float = 0.04`, `warmup_teacher_temp: float = 0.07`, `warmup_teacher_temp_epochs: int = 30`
     - `SupConConfig`: `temperature: float = 0.07`, `n_samples_per_class: int = 2`
   - Eval sub-configs (all inherit `_StrictBase`):
     - `LinearProbeConfig`: `max_epochs: int = 100`, `lr: float = 0.1`, `milestones: list[int] = [60, 80]`
     - `KNNConfig`: `k: int = 200`, `temperature: float = 0.07`, `every_n_epochs: int = 5`
     - `TSNEConfig`: `n_samples: int = 2000`, `perplexities: list[int] = [10, 30, 50]`
     - `UMAPConfig`: `n_samples: int = 5000`, `metric: str = "cosine"`
     - `FinetuneConfig`: `backbone_lr: float = 1e-4`, `head_lr: float = 1e-3`, `freeze_bn: bool = True`
     - `CAMConfig`: `method: str = "eigencam"`, `n_images: int = 8`
   - `EvalConfig(_StrictBase)`: `linear_probe: Optional[LinearProbeConfig] = None`, `knn: Optional[KNNConfig] = None`, `tsne: Optional[TSNEConfig] = None`, `umap: Optional[UMAPConfig] = None`, `finetune: Optional[FinetuneConfig] = None`, `cam: Optional[CAMConfig] = None`
   - `TrainConfig(_StrictBase)`:
     - `method: str`
     - `backbone: str = "resnet50"`
     - `pretrained: bool = False`
     - `max_epochs: int = 100`
     - `warmup_epochs: int = 10`
     - `batch_size: int = 256`
     - `lr: float = 0.3`
     - `weight_decay: float = 1e-6`
     - `optimizer: str = "adamw"` (Literal["adamw", "sgd", "lars"])
     - `scheduler: str = "warmup_cosine"` (Literal["warmup_cosine"])
     - `n_views: int = 2`
     - `data_dir: str = "data"`
     - `num_workers: int = 4`
     - Per-method optional sub-configs: `simclr: Optional[SimCLRConfig] = None`, `moco: Optional[MoCoConfig] = None`, `byol: Optional[BYOLConfig] = None`, `swav: Optional[SwAVConfig] = None`, `barlow_twins: Optional[BarlowTwinsConfig] = None`, `simsiam: Optional[SimSiamConfig] = None`, `dino: Optional[DINOConfig] = None`, `supcon: Optional[SupConConfig] = None`
     - `eval: Optional[EvalConfig] = None`
   - `load_config(path: str) -> TrainConfig` helper: `yaml.safe_load(open(path))` then `TrainConfig.model_validate(raw)`.

2. Create `configs/example.yaml`:
   ```yaml
   method: simclr_v1
   backbone: resnet18
   pretrained: false
   max_epochs: 100
   warmup_epochs: 10
   batch_size: 256
   lr: 0.3
   weight_decay: 1.0e-6
   optimizer: lars
   n_views: 2
   data_dir: data
   num_workers: 4

   simclr:
     temperature: 0.5
     projection_dim: 128

   eval:
     knn:
       k: 200
       temperature: 0.07
       every_n_epochs: 5
   ```

3. Create `tests/test_config.py` with all 7 tests described in the behavior block.
  </action>
  <verify>
    <automated>pytest tests/test_config.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - core/config.py contains `class _StrictBase(BaseModel)` with `model_config = ConfigDict(extra="forbid")`
    - core/config.py contains `class TrainConfig(_StrictBase)` with field `method: str`
    - core/config.py contains `class EvalConfig(_StrictBase)` with fields `linear_probe`, `knn`, `tsne`, `umap`, `finetune`, `cam`
    - core/config.py contains `def load_config(path: str) -> TrainConfig`
    - core/config.py contains `class SimCLRConfig(_StrictBase)` and at least 7 other method config classes
    - configs/example.yaml contains `method: simclr_v1`
    - tests/test_config.py contains at least 7 test functions
    - `pytest tests/test_config.py -x` exits 0
  </acceptance_criteria>
  <done>TrainConfig validates YAML with extra='forbid' on all levels; EvalConfig has all 6 sub-schemas; load_config works on example.yaml; all tests pass</done>
</task>

</tasks>

<verification>
- `python -c "from core.config import TrainConfig, EvalConfig, load_config; print('config OK')"` succeeds
- `pytest tests/test_config.py -x -v` — all 7+ tests pass
- `python -c "from core.config import load_config; c = load_config('configs/example.yaml'); print(c.method, c.simclr.temperature)"` prints `simclr_v1 0.5`
</verification>

<success_criteria>
- Project scaffold is in place: requirements.txt, pyproject.toml, all __init__.py files, conftest.py
- TrainConfig validates valid YAML and rejects unknown keys at all nesting levels
- EvalConfig sub-schema covers all 6 evaluation tools
- All config tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
