---
phase: 03-simclr
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - methods/simclr/module.py
  - tests/test_simclr.py
autonomous: true
requirements: [ERA2-03, ERA2-04]

must_haves:
  truths:
    - "SimCLRv1Module has complete DOC-02 docstring with paper, authors, venue, year, arXiv, gotchas, reference implementation"
    - "SimCLRv2Module has complete DOC-02 docstring with weight-decay sensitivity gotcha"
    - "Both methods train for multiple epochs via YAML configs without loss divergence"
    - "Full test suite passes including all prior phases"
  artifacts:
    - path: "methods/simclr/module.py"
      provides: "Completed module with DOC-02 docstrings"
      contains: "arXiv"
    - path: "tests/test_simclr.py"
      provides: "Smoke tests for both methods"
      contains: "test_simclr_v1_smoke_test"
  key_links:
    - from: "tests/test_simclr.py"
      to: "configs/simclr_v1_resnet18.yaml"
      via: "yaml.safe_load(open(config_path))"
      pattern: "simclr_v1_resnet18"
    - from: "tests/test_simclr.py"
      to: "configs/simclr_v2_resnet18.yaml"
      via: "yaml.safe_load(open(config_path))"
      pattern: "simclr_v2_resnet18"
---

<objective>
Complete DOC-02 docstrings for both SimCLR modules and add end-to-end smoke tests that verify both methods train successfully from YAML configs.

Purpose: Ensure both methods are fully documented per DOC-02 standards (paper, authors, venue, arXiv, gotchas, reference implementation) and verified end-to-end through smoke tests.
Output: Updated module.py with complete docstrings, additional smoke tests in test_simclr.py.
</objective>

<execution_context>
@/Users/yi-tingli/Documents/Projects/ml_topic_contrastive_learning/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yi-tingli/Documents/Projects/ml_topic_contrastive_learning/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-simclr/03-CONTEXT.md
@.planning/phases/03-simclr/03-01-SUMMARY.md
@.planning/phases/03-simclr/03-02-SUMMARY.md

<interfaces>
From methods/invariant_spread/module.py (DOC-02 reference):
```python
"""Invariant and Spreading Instance Feature (Ye et al., CVPR 2019).

In-batch contrastive learning without memory bank or queue. Two augmented
views of each image are produced; the model learns to bring views of the
same image together while pushing views of different images apart using
symmetric cross-entropy (InfoNCE) loss over in-batch instances.

Paper: "Unsupervised Embedding Learning via Invariant and Spreading Instance Feature"
Authors: Mang Ye, Xu Zhang, Pong C. Yuen, Shih-Fu Chang
Venue: CVPR 2019
arXiv: https://arxiv.org/abs/1904.03436

Reference implementation: https://github.com/mangye16/Unsupervised_Embedding_Learning
"""
# Module docstring also appears as class docstring with additional details:
# - Batch-size sensitivity note
# - Gotchas list
# - Reference implementation URL
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add complete DOC-02 docstrings to SimCLRv1Module and SimCLRv2Module</name>
  <files>methods/simclr/module.py</files>
  <read_first>
    methods/simclr/module.py
    methods/invariant_spread/module.py
    .planning/REQUIREMENTS.md
    .planning/phases/03-simclr/03-CONTEXT.md
  </read_first>
  <action>
    Update the module-level docstring and class docstrings in methods/simclr/module.py:

    **Module-level docstring:**
    ```
    """SimCLR v1 and v2 (Chen et al., ICML 2020 / NeurIPS 2020).

    Two-view in-batch contrastive learning with symmetric NT-Xent loss. Both views
    are encoded by a shared backbone and projected through an MLP head. Loss is
    computed on the projection output z; downstream evaluation uses the backbone
    representation h. Strong augmentation (color jitter s=1.0, Gaussian blur) is
    critical for performance.

    Paper (v1): "A Simple Framework for Contrastive Learning of Visual Representations"
    Authors: Ting Chen, Simon Kornblith, Mohammad Norouzi, Geoffrey Hinton
    Venue: ICML 2020
    arXiv: https://arxiv.org/abs/2002.05709

    Paper (v2): "Big Self-Supervised Models are Strong Semi-Supervised Learners"
    Authors: Ting Chen, Simon Kornblith, Kevin Swersky, Mohammad Norouzi, Geoffrey Hinton
    Venue: NeurIPS 2020
    arXiv: https://arxiv.org/abs/2006.10029

    Reference implementation: https://github.com/google-research/simclr
    """
    ```

    **SimCLRv1Module class docstring:**
    ```
    """SimCLR v1 (Chen et al., ICML 2020).

    A Simple Framework for Contrastive Learning of Visual Representations.

    Two augmented views of each image are encoded by a shared backbone and
    projected through a 2-layer MLP (2048->2048->128). The symmetric NT-Xent
    loss (implemented via InfoNCELoss with queue=None) brings views of the same
    image together while pushing views of different images apart within the
    batch. Loss is computed on the projection output z; downstream evaluation
    should use the backbone representation h.

    Paper: "A Simple Framework for Contrastive Learning of Visual Representations"
    Authors: Ting Chen, Simon Kornblith, Mohammad Norouzi, Geoffrey Hinton
    Venue: ICML 2020
    arXiv: https://arxiv.org/abs/2002.05709

    Algorithm:
    1. Augment each image twice with strong augmentation (s=1.0 color jitter,
       Gaussian blur, random grayscale, random crop + resize).
    2. Encode both views: h_i = backbone(x_i), h_j = backbone(x_j).
    3. Project: z_i = projector(h_i), z_j = projector(h_j).
    4. Compute symmetric NT-Xent loss on (z_i, z_j).

    Gotchas:
    - Color jitter strength must be s=1.0 (not torchvision default ~0.4).
      ContrastiveAugmentation(strong=True) handles this.
    - Performance degrades sharply below batch_size=256 because effective
      negatives = 2*(batch_size-1). Use LARS optimizer for batch sizes >1024.
    - Loss is computed on z (projection), but evaluation must use h (backbone).
      Do not evaluate downstream tasks on z.
    - InfoNCELoss internally L2-normalizes inputs; do not pre-normalize.

    Reference implementation: https://github.com/google-research/simclr
    """
    ```

    **SimCLRv2Module class docstring:**
    ```
    """SimCLR v2 (Chen et al., NeurIPS 2020).

    Big Self-Supervised Models are Strong Semi-Supervised Learners.

    Extends SimCLR v1 with a deeper 3-layer projection head
    (2048->2048->2048->128). Only the projection head depth changes;
    backbone, loss, and augmentation are identical to v1. This
    implementation covers the pretraining stage only; the semi-supervised
    distillation stage is deferred to v2 scope.

    Paper: "Big Self-Supervised Models are Strong Semi-Supervised Learners"
    Authors: Ting Chen, Simon Kornblith, Kevin Swersky, Mohammad Norouzi, Geoffrey Hinton
    Venue: NeurIPS 2020
    arXiv: https://arxiv.org/abs/2006.10029

    Gotchas:
    - Weight decay scales with projection head depth -- v1 fine-tune
      hyperparameters do not transfer directly to v2. The larger projection
      head (3 layers vs 2) requires more regularization.
    - The num_layers=2 -> num_layers=3 switch is controlled by this subclass,
      not by YAML config (per D-02).

    Reference implementation: https://github.com/google-research/simclr
    """
    ```
  </action>
  <verify>
    <automated>python -c "from methods.simclr.module import SimCLRv1Module, SimCLRv2Module; assert 'arXiv' in SimCLRv1Module.__doc__; assert 'arXiv' in SimCLRv2Module.__doc__; assert 'Gotcha' in SimCLRv1Module.__doc__ or 'gotcha' in SimCLRv1Module.__doc__.lower(); print('docstrings ok')"</automated>
  </verify>
  <acceptance_criteria>
    - methods/simclr/module.py module docstring contains "ICML 2020"
    - methods/simclr/module.py module docstring contains "NeurIPS 2020"
    - methods/simclr/module.py module docstring contains "arXiv"
    - SimCLRv1Module class docstring contains "Ting Chen"
    - SimCLRv1Module class docstring contains "arXiv: https://arxiv.org/abs/2002.05709"
    - SimCLRv1Module class docstring contains "batch_size=256" or "batch size"
    - SimCLRv1Module class docstring contains "s=1.0"
    - SimCLRv1Module class docstring contains "Reference implementation"
    - SimCLRv2Module class docstring contains "arXiv: https://arxiv.org/abs/2006.10029"
    - SimCLRv2Module class docstring contains "3-layer"
    - SimCLRv2Module class docstring contains "weight decay" or "Weight decay"
    - SimCLRv2Module class docstring contains "Reference implementation"
  </acceptance_criteria>
  <done>
    - Both module and class docstrings are complete per DOC-02 standard
    - Paper title, authors, venue, year, arXiv link present for both v1 and v2
    - Gotchas documented: batch-size sensitivity, s=1.0, loss on z vs eval on h, weight-decay for v2
    - Reference implementation URL included
  </done>
</task>

<task type="auto">
  <name>Task 2: Add end-to-end smoke tests for both SimCLR methods</name>
  <files>tests/test_simclr.py</files>
  <read_first>
    tests/test_simclr.py
    tests/test_invariant_spread.py
    configs/simclr_v1_resnet18.yaml
    configs/simclr_v2_resnet18.yaml
    methods/simclr/module.py
  </read_first>
  <action>
    Append smoke tests to tests/test_simclr.py (do not remove existing tests):

    1. `test_simclr_v1_smoke_3_epochs(tmp_imagefolder)`:
       - Load configs/simclr_v1_resnet18.yaml with yaml.safe_load
       - Override: data_dir=str(tmp_imagefolder), max_epochs=3, warmup_epochs=0, batch_size=4, num_workers=0
       - Validate with TrainConfig.model_validate(raw)
       - Register simclr_v1 if needed, create model via method_dispatcher(cfg)
       - Create SSLDataModule(data_dir=cfg.data_dir, n_views=2, batch_size=4, num_workers=0, size=32, strong=True)
       - Create LossTracker callback
       - Train 3 epochs with L.Trainer(max_epochs=3, accelerator="cpu", logger=False, enable_checkpointing=False, enable_progress_bar=False, callbacks=[tracker])
       - Assert all epoch losses are finite (not NaN, not >1e6)
       - Assert loss at last epoch < loss at first epoch (learning)

    2. `test_simclr_v2_smoke_3_epochs(tmp_imagefolder)`:
       - Same as above but with configs/simclr_v2_resnet18.yaml and method="simclr_v2"
       - Also assert that model.projector contains exactly 3 nn.Linear layers (confirming v2 head)

    3. `test_simclr_v1_docstring_has_doc02()`:
       - `from methods.simclr.module import SimCLRv1Module`
       - `doc = SimCLRv1Module.__doc__`
       - Assert doc contains "arXiv" (has paper link)
       - Assert doc contains "Gotcha" or "gotcha" (has gotcha list)
       - Assert doc contains "Reference implementation" (has reference URL)
       - Assert doc contains "ICML 2020" (has venue+year)

    4. `test_simclr_v2_docstring_has_doc02()`:
       - Same checks for SimCLRv2Module
       - Additionally assert doc contains "weight decay" or "Weight decay" (v2 gotcha per D-02)
       - Assert doc contains "NeurIPS 2020"
       - Assert doc contains "3-layer"
  </action>
  <verify>
    <automated>python -m pytest tests/test_simclr.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_simclr.py contains "def test_simclr_v1_smoke_3_epochs"
    - tests/test_simclr.py contains "def test_simclr_v2_smoke_3_epochs"
    - tests/test_simclr.py contains "def test_simclr_v1_docstring_has_doc02"
    - tests/test_simclr.py contains "def test_simclr_v2_docstring_has_doc02"
    - python -m pytest tests/test_simclr.py -x -q exits 0
    - python -m pytest tests/ -x -q exits 0 (full suite green)
  </acceptance_criteria>
  <done>
    - Smoke tests confirm both methods train 3 epochs without divergence
    - Docstring tests confirm DOC-02 compliance for both modules
    - Full test suite (all phases) remains green
    - Phase 3 is complete and ready for verification
  </done>
</task>

</tasks>

<verification>
python -m pytest tests/test_simclr.py -x -q
python -m pytest tests/ -x -q
python -c "from methods.simclr.module import SimCLRv1Module; assert 'arXiv' in SimCLRv1Module.__doc__; print('DOC-02 ok')"
</verification>

<success_criteria>
1. SimCLRv1Module and SimCLRv2Module have complete DOC-02 docstrings
2. Smoke tests confirm both methods train without divergence
3. Docstring tests verify DOC-02 compliance programmatically
4. Full test suite (all phases) passes
5. Phase 3 is complete — all 5 ROADMAP success criteria satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-simclr/03-03-SUMMARY.md`
</output>
