---
phase: 04-moco
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - configs/moco_v1_resnet18.yaml
  - configs/moco_v2_resnet18.yaml
  - methods/moco/module.py
  - tests/test_moco.py
autonomous: true
requirements:
  - ERA2-01
  - ERA2-02
  - INFRA-03

must_haves:
  truths:
    - "Per-method YAML configs exist for moco_v1 and moco_v2"
    - "YAML configs load and validate via TrainConfig.model_validate"
    - "DOC-02 docstrings include paper title, authors, venue, arXiv, algorithm summary, gotchas, and reference URL"
    - "MoCoV1Module docstring documents shuffled-BN limitation for multi-GPU"
    - "MoCoV1Module docstring documents m=0.9 vs m=0.999 sensitivity"
    - "End-to-end smoke tests pass: YAML config drives 3-epoch training"
  artifacts:
    - path: "configs/moco_v1_resnet18.yaml"
      provides: "MoCo v1 training config"
      contains: "method: moco_v1"
    - path: "configs/moco_v2_resnet18.yaml"
      provides: "MoCo v2 training config"
      contains: "method: moco_v2"
  key_links:
    - from: "configs/moco_v1_resnet18.yaml"
      to: "core/config.py"
      via: "TrainConfig.model_validate(yaml.safe_load(...))"
      pattern: "method: moco_v1"
    - from: "methods/moco/module.py"
      to: "DOC-02 standard"
      via: "class docstrings"
      pattern: "Paper:|Authors:|Venue:|arXiv:|Gotchas:"
---

<objective>
Add YAML configs, DOC-02 docstrings, and end-to-end smoke tests for MoCo v1 and v2.

Purpose: Complete the Phase 4 deliverables by making MoCo methods configurable via YAML, fully documented per DOC-02 standard, and verified end-to-end from config loading through training. This is the final plan that ties everything together.
Output: Two YAML configs in `configs/`, updated module docstrings in `methods/moco/module.py`, and smoke tests in `tests/test_moco.py`.
</objective>

<execution_context>
@/Users/yi-tingli/Documents/Projects/ml_topic_contrastive_learning/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yi-tingli/Documents/Projects/ml_topic_contrastive_learning/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-moco/04-CONTEXT.md
@.planning/phases/04-moco/04-RESEARCH.md
@.planning/phases/04-moco/04-02-SUMMARY.md

<interfaces>
<!-- Key types for YAML config validation. -->

From core/config.py — MoCoConfig fields:
```python
class MoCoConfig(_StrictBase):
    temperature: float = 0.07
    queue_size: int = 65536
    momentum: float = 0.999
```

From core/config.py — TrainConfig relevant fields:
```python
class TrainConfig(_StrictBase):
    method: str
    backbone: str = "resnet50"
    pretrained: bool = False
    max_epochs: int = 100
    warmup_epochs: int = 10
    batch_size: int = 256
    lr: float = 0.03
    weight_decay: float = 1e-4
    optimizer: str = "adamw"
    scheduler: str = "warmup_cosine"
    moco: MoCoConfig | None = None
    ...
```

From methods/simclr/module.py — DOC-02 docstring reference pattern:
```python
class SimCLRv1Module(BaseSSLModule):
    """SimCLR v1 (Chen et al., ICML 2020).

    A Simple Framework for Contrastive Learning of Visual Representations.

    [2-sentence algorithm description]

    Paper: "..."
    Authors: ...
    Venue: ICML 2020
    arXiv: https://arxiv.org/abs/...

    Algorithm:
    1. ...

    Gotchas:
    - ...

    Reference implementation: https://...
    """
```

Existing YAML config pattern (from configs/simclr_v1_resnet18_adamw.yaml or similar):
```yaml
method: simclr_v1
backbone: resnet18
pretrained: false
max_epochs: 200
warmup_epochs: 10
batch_size: 256
lr: 0.3
weight_decay: 1e-4
optimizer: adamw
simclr:
  temperature: 0.5
  projection_dim: 128
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: YAML configs and DOC-02 docstrings</name>
  <files>configs/moco_v1_resnet18.yaml, configs/moco_v2_resnet18.yaml, methods/moco/module.py</files>
  <action>
    **Create YAML configs:**

    1. `configs/moco_v1_resnet18.yaml`:
    ```yaml
    # MoCo v1 (He et al., CVPR 2020) — ResNet-18 tutorial config
    # Queue-based contrastive learning with momentum encoder.
    # Uses bare nn.Linear projection (128-dim), not MLP.
    # For batch sizes > 1024, consider LARS optimizer.
    method: moco_v1
    backbone: resnet18
    pretrained: false
    max_epochs: 200
    warmup_epochs: 10
    batch_size: 256
    lr: 0.03
    weight_decay: 1e-4
    optimizer: sgd
    scheduler: warmup_cosine
    moco:
      temperature: 0.07
      queue_size: 65536
      momentum: 0.999
    ```

    2. `configs/moco_v2_resnet18.yaml`:
    ```yaml
    # MoCo v2 (Chen et al., 2020) — ResNet-18 tutorial config
    # MoCo v1 + 2-layer MLP projection head + strong augmentation (Gaussian blur).
    # Uses cosine LR schedule. This is a "5-line diff from v1".
    # Strong augmentation (s=1.0 color jitter, Gaussian blur) is critical.
    method: moco_v2
    backbone: resnet18
    pretrained: false
    max_epochs: 200
    warmup_epochs: 10
    batch_size: 256
    lr: 0.03
    weight_decay: 1e-4
    optimizer: sgd
    scheduler: warmup_cosine
    moco:
      temperature: 0.07
      queue_size: 65536
      momentum: 0.999
    ```

    **Add DOC-02 docstrings to methods/moco/module.py:**

    Update the module-level docstring and both class docstrings following the SimCLR pattern exactly. The module docstring covers MoCo broadly; each class docstring covers its variant.

    Module docstring must include: MoCo v1 and v2 paper references, overview of queue-based contrastive learning approach.

    `MoCoV1Module` class docstring must include:
    - Paper: "Momentum Contrast for Unsupervised Visual Representation Learning"
    - Authors: Kaiming He, Haoqi Fan, Yuxin Wu, Saining Xie, Ross Girshick
    - Venue: CVPR 2020
    - arXiv: https://arxiv.org/abs/1911.05722
    - 2-sentence algorithm description (query encoder, momentum encoder, FIFO queue)
    - Algorithm steps (numbered)
    - Gotchas list:
      1. Shuffled BN for multi-GPU — not implemented, single-GPU only in this tutorial; the BN-leakage problem is what MoCo's shuffled BN solves (per D-05)
      2. m=0.9 produces significantly worse results than m=0.999 — lower momentum means target encoder diverges faster, reducing key consistency in the queue
      3. Queue must be updated AFTER loss computation, not before — otherwise current batch positive keys appear as negatives
      4. Momentum encoder parameters must never appear in optimizer param groups
    - Reference implementation: https://github.com/facebookresearch/moco

    `MoCoV2Module` class docstring must include:
    - Paper: "Improved Baselines with Momentum Contrastive Learning"
    - Authors: Xinlei Chen, Haoqi Fan, Ross Girshick, Kaiming He
    - Venue: arXiv 2020 (tech report)
    - arXiv: https://arxiv.org/abs/2003.04297
    - Note: "This is a 5-line diff from v1" (per D-02)
    - Describe what changed: 2-layer MLP head replacing FC, strong augmentation with Gaussian blur, cosine LR schedule
    - Gotcha: presents MoCo v2 as "SimCLR's architecture improvements applied to MoCo"
    - Reference implementation: https://github.com/facebookresearch/moco
  </action>
  <verify>
    <automated>python -c "
import yaml
from core.config import TrainConfig
for name in ['moco_v1_resnet18', 'moco_v2_resnet18']:
    with open(f'configs/{name}.yaml') as f:
        cfg = TrainConfig.model_validate(yaml.safe_load(f))
    print(f'{name}: method={cfg.method}, moco.queue_size={cfg.moco.queue_size}')
print('OK')
"</automated>
  </verify>
  <done>Both YAML configs load and validate. DOC-02 docstrings are complete with paper metadata, algorithm description, gotchas, and reference URLs.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end smoke tests and docstring validation</name>
  <files>tests/test_moco.py</files>
  <action>
    Add smoke tests to `tests/test_moco.py` (append to existing test file from Plan 02):

    1. **YAML smoke tests** (`test_moco_v1_yaml_smoke`, `test_moco_v2_yaml_smoke`):
    - Load the YAML config via `load_config("configs/moco_v1_resnet18.yaml")`
    - Override: set `max_epochs=3`, `batch_size=16`, and `moco.queue_size=64` for speed
    - Create `SSLDataModule` with `large_imagefolder`, `n_views=2`, `size=32`, `strong=True`
    - Instantiate module via `method_dispatcher(cfg)` or directly
    - Train 3 epochs with `L.Trainer(max_epochs=3, accelerator="cpu", logger=False, enable_checkpointing=False)`
    - Assert training completes without error
    - Same for moco_v2

    2. **Docstring validation tests** (`test_moco_v1_docstring`, `test_moco_v2_docstring`):
    - Check that class docstring contains required DOC-02 fields:
      - "Paper:" or paper title
      - "Authors:"
      - "Venue:"
      - "arXiv:"
      - "Gotchas:" or "Gotcha"
      - "Reference implementation:"
    - For MoCoV1Module, also check "shuffled" appears (shuffled-BN documentation)
    - For MoCoV2Module, also check "5-line diff" appears

    Follow the test patterns established in Phase 03 (test_simclr.py).
  </action>
  <verify>
    <automated>python -m pytest tests/test_moco.py -x -q</automated>
  </verify>
  <done>End-to-end smoke tests pass for both methods driven by YAML configs. Docstring validation confirms DOC-02 compliance for both MoCoV1Module and MoCoV2Module.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_moco.py -x -q` — all MoCo tests pass (unit + smoke + docstring)
- `python -m pytest -x -q` — full test suite green (no regressions)
- Both YAML configs load via `TrainConfig.model_validate`
</verification>

<success_criteria>
- Two YAML configs exist and validate: `configs/moco_v1_resnet18.yaml`, `configs/moco_v2_resnet18.yaml`
- Both module docstrings meet DOC-02 standard (paper, authors, venue, arXiv, gotchas, reference)
- MoCoV1Module docstring documents shuffled-BN limitation and m=0.9 vs m=0.999 sensitivity
- MoCoV2Module docstring documents "5-line diff from v1"
- Smoke tests pass: YAML-driven 3-epoch training for both methods
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-moco/04-03-SUMMARY.md`
</output>
