---
phase: 10
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - methods/swav/module.py
  - methods/infomin/module.py
  - methods/byol/module.py
  - methods/simsiam/module.py
  - methods/barlow_twins/module.py
  - methods/dino/module.py
  - methods/supcon/module.py
  - tests/test_docstrings.py
autonomous: true
requirements:
  - DOC-02
must_haves:
  truths:
    - "Every LightningModule subclass class-level docstring contains: paper title, authors, venue, year, arXiv link, 2-sentence description, gotcha list, reference implementation URL"
    - "Programmatic test (tests/test_docstrings.py) verifies DOC-02 compliance for all 15 dispatcher-registered classes"
    - "SwAVModule class docstring is no longer a one-liner placeholder pointing at the module docstring"
    - "SupConModule and SupConFinetuneModule have a Gotchas section listing the class-balanced sampler / sum-outside / weight_decay=0.0 traps"
  artifacts:
    - path: "tests/test_docstrings.py"
      provides: "DOC-02 compliance checker covering all 15 LightningModule subclasses"
      contains: "_check_doc02"
    - path: "methods/swav/module.py"
      provides: "Full DOC-02 class docstring on SwAVModule (replaces placeholder)"
      contains: "Reference implementation"
    - path: "methods/infomin/module.py"
      provides: "Full DOC-02 class docstring on InfoMinModule"
      contains: "arXiv:"
    - path: "methods/byol/module.py"
      provides: "Full DOC-02 class docstring on BYOLModule (Paper/Authors/Venue/arXiv/Algorithm/Gotchas/RefImpl)"
      contains: "Algorithm:"
    - path: "methods/simsiam/module.py"
      provides: "Full DOC-02 class docstring on SimSiamModule"
      contains: "Gotchas:"
    - path: "methods/barlow_twins/module.py"
      provides: "Full DOC-02 class docstring on BarlowTwinsModule"
      contains: "arXiv:"
    - path: "methods/dino/module.py"
      provides: "Full DOC-02 class docstring on DINOModule"
      contains: "Reference implementation"
    - path: "methods/supcon/module.py"
      provides: "Full DOC-02 class docstrings on SupConModule and SupConFinetuneModule with new Gotchas sections"
      contains: "ClassBalancedSampler"
  key_links:
    - from: "tests/test_docstrings.py"
      to: "all 15 LightningModule subclasses"
      via: "explicit imports + _check_doc02 helper"
      pattern: "from methods\\.\\w+\\.module import"
---

<objective>
Bring all LightningModule class docstrings up to DOC-02 compliance and lock in the standard
with an automated test (`tests/test_docstrings.py`).

Per the Phase 10 research audit (10-RESEARCH.md):
- 7 of 15 classes are already DOC-02 compliant at the class level
  (InstanceDiscriminationModule, InvariantSpreadModule, SimCLRv1Module, SimCLRv2Module,
  MoCoV1Module, MoCoV2Module, MoCoV3Module).
- 8 classes have DOC-02 fields in module docstrings but NOT in class docstrings — DOC-02
  requires the **class** docstring per REQUIREMENTS.md.
  The 8: SwAVModule, InfoMinModule, BYOLModule, SimSiamModule, BarlowTwinsModule, DINOModule,
  SupConModule, SupConFinetuneModule.
- SwAVModule's current class docstring is literally a placeholder one-liner
  (`"""SwAVModule (see module-level docstring for full DOC-02 documentation)."""`) that
  must be completely rewritten.
- SupConModule + SupConFinetuneModule additionally have NO Gotchas section in either the
  module or class docstring — must be written fresh from REQUIREMENTS.md content.

Purpose: Satisfy DOC-02. Make per-method documentation discoverable via `help(MyMethod)` /
IDE tooltips / Sphinx (without delegating to module-level docs).

Output: 7 class docstrings rewritten/extended, 1 file (`tests/test_docstrings.py`) created
with 15 per-class assertions, full test suite green.
</objective>

<execution_context>
@/Users/yi-tingli/Documents/Projects/ml_topic_contrastive_learning/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yi-tingli/Documents/Projects/ml_topic_contrastive_learning/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/10-documentation-and-tutorial/10-RESEARCH.md
@.planning/phases/10-documentation-and-tutorial/10-PATTERNS.md
@methods/simclr/module.py
@methods/swav/module.py
@methods/infomin/module.py
@methods/byol/module.py
@methods/simsiam/module.py
@methods/barlow_twins/module.py
@methods/dino/module.py
@methods/supcon/module.py
@tests/test_simclr.py
@tests/test_smoke_transformer.py

<interfaces>
<!-- DOC-02 reference structure (from SimCLRv1Module — already compliant) -->

The DOC-02 standard class docstring template:
```python
class <Method>Module(BaseSSLModule):
    """<Method> (<Authors-Short>, <Venue> <Year>).

    <Full paper title>.

    <Sentence 1: what the method does architecturally.>
    <Sentence 2: what loss/mechanism drives learning.>

    Paper: "<Full Paper Title>"
    Authors: <Author 1>, <Author 2>, ...
    Venue: <Conference/Journal> <Year>
    arXiv: https://arxiv.org/abs/<id>

    Algorithm:
    1. <Step 1>
    2. <Step 2>
    ...

    Gotchas:
    - <Gotcha 1>
    - <Gotcha 2>
    ...

    Reference implementation: https://github.com/<repo>
    """
```

Required field markers (string-matched by tests/test_docstrings.py):
- `Paper:`
- `Authors:`
- `Venue:`
- `arXiv:` (prefix; full URL `https://arxiv.org/abs/<id>` follows)
- `Gotcha` (case-insensitive — matches `Gotchas:`)
- `Reference implementation`
- The venue/year string (e.g., `ICML 2020`)
- The arXiv ID fragment (e.g., `2002.05709`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite class docstrings for SwAV, InfoMin, BYOL, SimSiam, BarlowTwins, DINO, SupCon, SupConFinetune</name>
  <files>
    methods/swav/module.py,
    methods/infomin/module.py,
    methods/byol/module.py,
    methods/simsiam/module.py,
    methods/barlow_twins/module.py,
    methods/dino/module.py,
    methods/supcon/module.py
  </files>
  <read_first>
    - methods/simclr/module.py (gold-standard SimCLRv1Module class docstring — copy structure exactly)
    - methods/swav/module.py (current placeholder + module docstring with source content)
    - methods/infomin/module.py (current class docstring + module docstring source content)
    - methods/byol/module.py (current partial class docstring + module docstring with full DOC-02 fields)
    - methods/simsiam/module.py (current state)
    - methods/barlow_twins/module.py (current state)
    - methods/dino/module.py (current architecture-only class docstring + module docstring with DOC-02 fields)
    - methods/supcon/module.py (current state of both SupConModule and SupConFinetuneModule)
    - .planning/phases/10-documentation-and-tutorial/10-PATTERNS.md (File 3: full source content for each docstring rewrite, including the SupCon-specific Gotchas content)
    - .planning/REQUIREMENTS.md (ERA2-05 SwAV, ERA2-06 InfoMin, ERA3-01 BYOL, ERA3-02 SimSiam, ERA3-03 Barlow Twins, ERA4-02 DINO, SUP-01 SupCon — these contain the canonical Gotcha content)
  </read_first>
  <action>
    For each of the seven files below, REPLACE the existing class docstring with a fully
    DOC-02 compliant docstring. Keep the module-level docstring as-is — the goal is to
    duplicate the DOC-02 content into the class docstring (where DOC-02 says it must live),
    NOT to delete it from the module.

    The existing module-level docstrings already contain all the field content needed. Use
    them as the source of truth for paper/authors/venue/arXiv/gotchas, then format into the
    class docstring template above.

    ### File 1: `methods/swav/module.py`

    Current class docstring (line ~48): `"""SwAV module (see module-level docstring for full DOC-02 documentation)."""`

    Replace with:
    ```python
    class SwAVModule(BaseSSLModule):
        """SwAV (Caron et al., NeurIPS 2020).

        Unsupervised Learning of Visual Features by Contrasting Cluster Assignments.

        Online clustering replaces in-batch contrastive negatives with assignments
        to a learnable codebook of K prototype vectors. The swapped-prediction loss
        forces the assignment for one augmented view to be predictable from the
        features of another view, while Sinkhorn-Knopp optimal transport enforces
        equal prototype usage across the batch.

        Paper: "Unsupervised Learning of Visual Features by Contrasting Cluster Assignments"
        Authors: Mathilde Caron, Ishan Misra, Julien Mairal, Priya Goyal,
                 Piotr Bojanowski, Armand Joulin
        Venue: NeurIPS 2020
        arXiv: https://arxiv.org/abs/2006.09882

        Algorithm:
        1. Multi-crop: produce 2 large (224x224) + N small (96x96) crops per image.
        2. Encode all crops through shared backbone + 2-layer projector; L2-normalize.
        3. Compute prototype scores (normalized features @ prototype matrix).
        4. For each large crop, compute soft assignment codes via Sinkhorn-Knopp OT.
        5. Swapped prediction: all other crops predict each large crop's codes.
        6. Loss is cross-entropy between predictions and codes, averaged over pairs.

        Gotchas:
        - Prototypes must be frozen for the first `freeze_prototypes_epochs` epochs
          (default: 1). Training without this causes unstable cluster assignments.
        - Prototype vectors must be L2-normalized after every optimizer step. Without
          this, score magnitudes grow unboundedly and Sinkhorn-Knopp overflows.
        - Sinkhorn-Knopp code matrix must be doubly stochastic — each prototype
          gets equal assignment mass. Use epsilon=0.05 (or 0.03 if unstable).
        - Memory usage with 8 crops is ~4x SimCLR. Reduce batch_size to 1/4 of
          SimCLR batch size (e.g., 64 vs 256 for ResNet-18).

        Reference implementation: https://github.com/facebookresearch/swav
        """
    ```

    ### File 2: `methods/infomin/module.py`

    Replace the existing `InfoMinModule` class docstring with:
    ```python
    class InfoMinModule(BaseSSLModule):
        """InfoMin (Tian et al., NeurIPS 2020).

        What Makes for Good Views for Contrastive Learning?

        InfoMin is presented here as an augmentation-policy demonstration on top of
        the SimCLR/MoCo v2 backbone. The principle: views should share minimal
        mutual information except for task-relevant content. The implementation
        substitutes aggressive color jitter + random grayscale (and removes Gaussian
        blur) into the standard contrastive pipeline to approximate the "minimal-MI"
        recipe without the full semi-supervised view-learning framework.

        Paper: "What Makes for Good Views for Contrastive Learning?"
        Authors: Yonglong Tian, Chen Sun, Ben Poole, Dilip Krishnan,
                 Cordelia Schmid, Phillip Isola
        Venue: NeurIPS 2020
        arXiv: https://arxiv.org/abs/2005.10243

        Algorithm:
        1. Wrap a SimCLR-style backbone with the "InfoMin" augmentation policy
           (aggressive color jitter, random grayscale p=0.5, no Gaussian blur).
        2. Encode two augmented views and project via 2-layer MLP head.
        3. Compute symmetric NT-Xent loss as in SimCLR.

        Gotchas:
        - This module is the augmentation-policy demonstration only; the full
          semi-supervised view-learning framework is v2 scope (see V2-06).
        - Without the aggressive color jitter, the InfoMin recipe collapses to
          ordinary SimCLR — verify augmentation strength via tools/compare_augmentations.py.
        - Performance is sensitive to dataset domain — the default policy is tuned
          for natural images; medical/industrial domains may need different views.

        Reference implementation: https://github.com/HobbitLong/PyContrast
        """
    ```

    ### File 3: `methods/byol/module.py`

    The current class docstring has the architecture description but is missing the
    Paper/Authors/Venue/arXiv/Algorithm/Reference implementation fields. Replace the
    full class docstring with:
    ```python
    class BYOLModule(BaseSSLModule):
        """BYOL (Grill et al., NeurIPS 2020).

        Bootstrap Your Own Latent — A New Approach to Self-Supervised Learning.

        Two networks (online and target) process two augmented views of an image.
        The online network has an extra predictor MLP on top of the projector; the
        target network is an EMA copy with no predictor. The MSE loss between the
        L2-normalized online prediction and L2-normalized target projection
        (equivalent to 2 - 2*cos_sim), combined with the predictor asymmetry and
        EMA target, prevents representational collapse without any negatives.

        Paper: "Bootstrap Your Own Latent: A New Approach to Self-Supervised Learning"
        Authors: Jean-Bastien Grill, Florian Strub, Florent Altche, Corentin Tallec,
                 Pierre H. Richemond, Elena Buchatskaya, Carl Doersch,
                 Bernardo Avila Pires, Zhaohan Daniel Guo, Mohammad Gheshlaghi Azar,
                 Bilal Piot, Koray Kavukcuoglu, Remi Munos, Michal Valko
        Venue: NeurIPS 2020
        arXiv: https://arxiv.org/abs/2006.07733

        Algorithm:
        1. Augment image x into views v1, v2.
        2. Online: y1 = backbone(v1); z1 = projector(y1); p1 = predictor(z1).
        3. Target: y2' = backbone_ema(v2); z2' = projector_ema(y2').
        4. Loss = ||normalize(p1) - normalize(z2'.detach())||^2 (and the symmetric pair).
        5. Update online via gradient; update target via cosine-scheduled EMA (0.996 -> 1.0).

        Gotchas:
        - The predictor is on the ONLINE branch only. The target branch has no
          predictor — adding one breaks the asymmetry that prevents collapse.
        - EMA momentum must be cosine-scheduled (0.996 -> 1.0), not constant.
          Constant momentum (e.g., m=0.99) leads to suboptimal representations.
        - Target branch parameters must have requires_grad=False and be excluded
          from the optimizer via the `learnable_params` property. Including them
          silently wastes compute and corrupts training.
        - L2-normalize projector outputs BEFORE the loss (and the predictor input
          internally also L2-normalizes its target).
        - EMA update must run in `on_train_batch_end`, never `training_step` or
          `on_before_optimizer_step`.

        Reference implementation: https://github.com/deepmind/deepmind-research/tree/master/byol
        """
    ```

    ### File 4: `methods/simsiam/module.py`

    Replace the SimSiamModule class docstring with:
    ```python
    class SimSiamModule(BaseSSLModule):
        """SimSiam (Chen & He, CVPR 2021).

        Exploring Simple Siamese Representation Learning.

        SimSiam is BYOL without the EMA target — a single shared encoder produces
        both views, and the only thing preventing collapse is the predictor MLP
        plus a stop-gradient on the projector output. The symmetric negative-cosine
        loss `-(cos_sim(p1, z2.detach()) + cos_sim(p2, z1.detach())) / 2` is
        computed on L2-normalized vectors.

        Paper: "Exploring Simple Siamese Representation Learning"
        Authors: Xinlei Chen, Kaiming He
        Venue: CVPR 2021
        arXiv: https://arxiv.org/abs/2011.10566

        Algorithm:
        1. Augment x into v1, v2; encode both with the shared backbone.
        2. Project: z1 = projector(h1); z2 = projector(h2). 3-layer projector with
           BN on all layers including the output (no ReLU on output).
        3. Predict: p1 = predictor(z1); p2 = predictor(z2). Bottleneck 2-layer MLP
           (2048 -> 512 -> 2048).
        4. Loss = -(cos_sim(p1, z2.detach()) + cos_sim(p2, z1.detach())) / 2.

        Gotchas:
        - Forgetting `.detach()` causes immediate collapse to loss = -1.0 plateau —
          this is the single most common SimSiam implementation bug.
        - The final projector layer has BN but NO ReLU. Adding ReLU here measurably
          hurts performance.
        - Use SGD (NOT LARS); paper uses lr = 0.05 * batch_size / 256.
        - Mandatory training diagnostic: log z.std(dim=0).mean(); should stay near
          0.707. Approaching 0 indicates collapse.

        Reference implementation: https://github.com/facebookresearch/simsiam
        """
    ```

    ### File 5: `methods/barlow_twins/module.py`

    Replace the BarlowTwinsModule class docstring with:
    ```python
    class BarlowTwinsModule(BaseSSLModule):
        """Barlow Twins (Zbontar et al., ICML 2021).

        Barlow Twins: Self-Supervised Learning via Redundancy Reduction.

        Two augmented views are encoded by a shared backbone and a high-dimensional
        projector (8192-dim output). The cross-correlation matrix C between the
        projected representations of the two batches (normalized by batch size) is
        driven toward the identity: diagonal terms invariance ((1-C_ii)^2) and
        off-diagonal terms redundancy (lambda * C_ij^2). No negatives, no EMA, no
        predictor — fully symmetric.

        Paper: "Barlow Twins: Self-Supervised Learning via Redundancy Reduction"
        Authors: Jure Zbontar, Li Jing, Ishan Misra, Yann LeCun, Stephane Deny
        Venue: ICML 2021
        arXiv: https://arxiv.org/abs/2103.03230

        Algorithm:
        1. Augment x into v1, v2; encode both with shared backbone.
        2. Project to 8192-dim via 3-layer MLP (BN+ReLU on layers 1-2, BN-only on 3).
        3. Standardize (zero mean, unit variance) projector outputs across batch.
        4. Compute cross-correlation C = Z1.T @ Z2 / N.
        5. Loss = sum_i (1 - C_ii)^2 + lambda * sum_{i!=j} C_ij^2 (lambda=5e-3).

        Gotchas:
        - C must be normalized by batch size (divide by N) before computing the loss.
          Without this, the loss scale depends on batch size in a non-obvious way.
        - Projector output must be standardized (zero mean, unit variance per
          dimension across the batch) before computing C. BN at the projector output
          handles standardization but it must happen at loss-computation time too.
        - lambda is sensitive — values much above 5e-3 push C off-diagonals to zero
          but starve the diagonal; values much below leave redundancy uncontrolled.
        - Do not reduce projector output dim below 2048 without empirical validation.
        - Mandatory training diagnostic: log z.std(dim=0).mean(); collapse signal.

        Reference implementation: https://github.com/facebookresearch/barlowtwins
        """
    ```

    ### File 6: `methods/dino/module.py`

    Replace the DINOModule class docstring with:
    ```python
    class DINOModule(BaseSSLModule):
        """DINO (Caron et al., ICCV 2021).

        Emerging Properties in Self-Supervised Vision Transformers.

        Self-distillation with a student-teacher framework where both networks share
        the same ViT architecture. The teacher is an EMA copy of the student and
        receives only global crops; the student receives all crops (global + local).
        Loss is cross-entropy between student and teacher softmax outputs over a
        large prototype layer (65,536-dim). Collapse is prevented by centering
        (running mean subtracted from the teacher output) plus sharpening (low
        teacher temperature).

        Paper: "Emerging Properties in Self-Supervised Vision Transformers"
        Authors: Mathilde Caron, Hugo Touvron, Ishan Misra, Herve Jegou,
                 Julien Mairal, Piotr Bojanowski, Armand Joulin
        Venue: ICCV 2021
        arXiv: https://arxiv.org/abs/2104.14294

        Algorithm:
        1. Multi-crop: 2 global (224x224) + 6-10 local (96x96) crops per image.
        2. Student processes all crops; teacher processes only global crops.
        3. Student/teacher each compute softmax over a 65536-dim prototype layer.
        4. Centering: teacher output - running_center (updated each step from teacher).
        5. Sharpening: teacher uses low temperature (warmup 0.04 -> 0.07).
        6. Loss = sum over all (student crop, teacher global crop) pairs of
           cross-entropy(student_softmax, teacher_softmax_centered_sharpened).

        Gotchas:
        - The centering vector must be updated with teacher outputs BEFORE computing
          the loss. Updating after the loss leaks one step of stale center.
        - Teacher temperature MUST warm up (0.04 -> 0.07). Starting at 0.07
          immediately produces early instability.
        - Local crops MUST NOT be passed to the teacher — teacher receives global
          crops only. Passing local crops to teacher changes the objective semantics.
        - Gradient clipping (max_norm=3.0) is required for stable ViT training.
        - 65,536-dim prototype layer is important — reducing it measurably degrades
          quality on ImageNet.

        Reference implementation: https://github.com/facebookresearch/dino
        """
    ```

    ### File 7: `methods/supcon/module.py` — TWO classes

    Replace the SupConModule class docstring:
    ```python
    class SupConModule(BaseSSLModule):
        """SupCon (Khosla et al., NeurIPS 2020).

        Supervised Contrastive Learning.

        Stage-1 pretraining: encodes two augmented views through a shared backbone
        and 2-layer MLP projection head. Unlike SimCLR, the SupCon loss treats
        every in-batch image with the same class label as a positive for each
        anchor (not just the other augmented view). Generalizes to SimCLR when
        labels are absent (degenerate case with one positive per anchor).

        Paper: "Supervised Contrastive Learning"
        Authors: Prannay Khosla, Piotr Teterwak, Chen Wang, Aaron Sarna,
                 Yonglong Tian, Phillip Isola, Aaron Maschinot, Ce Liu, Dilip Krishnan
        Venue: NeurIPS 2020
        arXiv: https://arxiv.org/abs/2004.11362

        Algorithm:
        1. ClassBalancedSampler builds batches with >=2 instances per class.
        2. Augment each image into 2 views; encode + project both via shared head.
        3. Compute SupConLoss with sum-outside formulation (Eq. 2 of paper):
           loss_i = -1/|P(i)| * sum_{p in P(i)} [s_{ip}/tau - log sum_{a!=i} exp(s_{ia}/tau)]
           where P(i) is the set of all in-batch images with the same label as i.

        Gotchas:
        - Do NOT add a classifier during stage-1 pretraining. Adding cross-entropy
          alongside the SupCon loss collapses the contrastive representation.
        - ClassBalancedSampler is REQUIRED. Without >=2 instances per class per batch,
          singleton classes have no positives and SupCon degenerates to SimCLR.
        - Use the sum-outside formulation (Eq. 2), not sum-inside. Sum-inside is
          a recurring implementation bug that gives different gradients.
        - Features are L2-normalized internally by SupConLoss; do not pre-normalize.
        - This is stage 1 only. Use SupConFinetuneModule (configs/supcon_stage2_*.yaml)
          for stage-2 linear classifier training.

        Reference implementation: https://github.com/HobbitLong/SupContrast
        """
    ```

    Replace the SupConFinetuneModule class docstring:
    ```python
    class SupConFinetuneModule(BaseSSLModule):
        """SupCon stage-2 fine-tuning (Khosla et al., NeurIPS 2020).

        Supervised Contrastive Learning — linear classifier on frozen backbone.

        Loads the stage-1 SupCon checkpoint, freezes the backbone (requires_grad=False
        and BN layers in eval mode), and trains a linear classifier on top with SGD
        and weight_decay=0.0. The frozen backbone keeps the contrastive
        representation intact while the linear head learns the class boundary.

        Paper: "Supervised Contrastive Learning"
        Authors: Prannay Khosla, Piotr Teterwak, Chen Wang, Aaron Sarna,
                 Yonglong Tian, Phillip Isola, Aaron Maschinot, Ce Liu, Dilip Krishnan
        Venue: NeurIPS 2020
        arXiv: https://arxiv.org/abs/2004.11362

        Algorithm:
        1. Load stage-1 SupCon checkpoint via cfg.ckpt_path or --ckpt-path CLI flag.
        2. Freeze backbone: requires_grad=False on all backbone params; BN -> eval().
        3. Build a linear head (nn.Linear(feat_dim, num_classes)).
        4. Train with SGD (lr ~ 0.1, momentum 0.9, weight_decay=0.0).
        5. Use one augmented view per image (no contrastive views needed).

        Gotchas:
        - Use SGD with weight_decay=0.0. Any nonzero weight decay regularizes the
          linear head away from the signal and measurably suppresses accuracy.
        - Freeze backbone AFTER loading the stage-1 checkpoint, not before. Freezing
          before load_state_dict can interfere with checkpoint key matching.
        - BN layers in the frozen backbone MUST remain in eval() mode through
          training. The freeze hook must explicitly call backbone.eval() each step.
        - Use only one view (views[0]) during stage-2; multi-view contrastive is
          irrelevant to linear classifier training.

        Reference implementation: https://github.com/HobbitLong/SupContrast
        """
    ```

    Implementation notes:
    - Do NOT delete the existing module-level docstring at the top of any file.
    - Do NOT modify any code below the class docstring (no behavior changes).
    - Verify docstrings render correctly via `python -c "import methods.<x>.module; help(<X>Module)"`.
    - The Authors line for SupCon was inaccurately written in the existing module
      docstring ("Piyush Tian" etc.) — the correct author list is on the arXiv page;
      use the accurate version above. The module docstring may be left as-is or
      corrected at executor's discretion (out of scope for DOC-02).
  </action>
  <verify>
    <automated>python -c "from methods.swav.module import SwAVModule; from methods.infomin.module import InfoMinModule; from methods.byol.module import BYOLModule; from methods.simsiam.module import SimSiamModule; from methods.barlow_twins.module import BarlowTwinsModule; from methods.dino.module import DINOModule; from methods.supcon.module import SupConModule, SupConFinetuneModule; import sys; [sys.exit(1) for c in [SwAVModule, InfoMinModule, BYOLModule, SimSiamModule, BarlowTwinsModule, DINOModule, SupConModule, SupConFinetuneModule] if 'arXiv:' not in (c.__doc__ or '') or 'Reference implementation' not in (c.__doc__ or '') or 'Gotcha' not in (c.__doc__ or '').lower().replace('gotcha', 'Gotcha')]"</automated>
  </verify>
  <acceptance_criteria>
    - SwAVModule class docstring no longer contains the placeholder text "see module-level docstring": `! grep -F "see module-level docstring for full DOC-02" methods/swav/module.py`
    - SwAVModule class docstring contains `arXiv: https://arxiv.org/abs/2006.09882`: `python -c "from methods.swav.module import SwAVModule; assert '2006.09882' in SwAVModule.__doc__"`
    - SwAVModule class docstring contains `Gotchas:`: `python -c "from methods.swav.module import SwAVModule; assert 'Gotchas:' in SwAVModule.__doc__"`
    - SwAVModule class docstring contains `Reference implementation:`: `python -c "from methods.swav.module import SwAVModule; assert 'Reference implementation' in SwAVModule.__doc__"`
    - InfoMinModule class docstring contains `arXiv: https://arxiv.org/abs/2005.10243`: `python -c "from methods.infomin.module import InfoMinModule; assert '2005.10243' in InfoMinModule.__doc__"`
    - BYOLModule class docstring contains `Paper:`, `Authors:`, `Venue:`, `arXiv:`, `Algorithm:`, `Gotchas:`, `Reference implementation`: `python -c "from methods.byol.module import BYOLModule; d = BYOLModule.__doc__; [assert_x for assert_x in [('Paper:' in d), ('Authors:' in d), ('Venue:' in d), ('arXiv:' in d), ('Algorithm:' in d), ('Gotchas:' in d), ('Reference implementation' in d)] if not assert_x]; assert all([('Paper:' in d), ('Authors:' in d), ('Venue:' in d), ('arXiv:' in d), ('Algorithm:' in d), ('Gotchas:' in d), ('Reference implementation' in d)])"`
    - SimSiamModule class docstring contains `arXiv: https://arxiv.org/abs/2011.10566`: `python -c "from methods.simsiam.module import SimSiamModule; assert '2011.10566' in SimSiamModule.__doc__"`
    - BarlowTwinsModule class docstring contains `arXiv: https://arxiv.org/abs/2103.03230`: `python -c "from methods.barlow_twins.module import BarlowTwinsModule; assert '2103.03230' in BarlowTwinsModule.__doc__"`
    - DINOModule class docstring contains `arXiv: https://arxiv.org/abs/2104.14294`: `python -c "from methods.dino.module import DINOModule; assert '2104.14294' in DINOModule.__doc__"`
    - SupConModule class docstring contains `arXiv: https://arxiv.org/abs/2004.11362` and `ClassBalancedSampler is REQUIRED`: `python -c "from methods.supcon.module import SupConModule; d = SupConModule.__doc__; assert '2004.11362' in d and 'ClassBalancedSampler' in d"`
    - SupConFinetuneModule class docstring contains `weight_decay=0.0` gotcha: `python -c "from methods.supcon.module import SupConFinetuneModule; d = SupConFinetuneModule.__doc__; assert 'weight_decay=0.0' in d"`
    - No regressions: `pytest tests/ -q` returns exit 0 (existing 326 tests still pass — docstring changes do not affect behavior)
  </acceptance_criteria>
  <done>
    All 8 previously non-compliant LightningModule subclasses now have full DOC-02
    class-level docstrings. Module-level docstrings are preserved. No code behavior changed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/test_docstrings.py — DOC-02 compliance test for all 15 LightningModule subclasses</name>
  <files>tests/test_docstrings.py</files>
  <read_first>
    - .planning/phases/10-documentation-and-tutorial/10-PATTERNS.md (File 5: full test file template + per-class arXiv ID table)
    - tests/test_simclr.py (lines 493-514: existing test_simclr_v1_docstring_has_doc02 pattern — model for individual checks)
    - tests/test_smoke_transformer.py (lines 217-276: existing test_doc02_moco_v3 / test_doc02_dino — these merge module + class docs; new test must check class doc only)
    - methods/swav/module.py through methods/supcon/module.py (verify class docstrings match what _check_doc02 will assert after Task 1)
  </read_first>
  <behavior>
    - Test 1 (`test_doc02_instance_discrimination`): InstanceDiscriminationModule.__doc__ contains 'CVPR 2018' and '1805.01978'
    - Test 2 (`test_doc02_invariant_spread`): InvariantSpreadModule.__doc__ contains 'CVPR 2019' and 'arXiv'
    - Test 3 (`test_doc02_simclr_v1`): SimCLRv1Module.__doc__ contains 'ICML 2020' and '2002.05709'
    - Test 4 (`test_doc02_simclr_v2`): SimCLRv2Module.__doc__ contains 'NeurIPS 2020' and '2006.10029'
    - Test 5 (`test_doc02_moco_v1`): MoCoV1Module.__doc__ contains 'CVPR 2020' and '1911.05722'
    - Test 6 (`test_doc02_moco_v2`): MoCoV2Module.__doc__ contains '2020' and '2003.04297'
    - Test 7 (`test_doc02_swav`): SwAVModule.__doc__ contains 'NeurIPS 2020' and '2006.09882' (NO LONGER placeholder)
    - Test 8 (`test_doc02_infomin`): InfoMinModule.__doc__ contains 'NeurIPS 2020' and '2005.10243'
    - Test 9 (`test_doc02_byol`): BYOLModule.__doc__ contains 'NeurIPS 2020' and '2006.07733'
    - Test 10 (`test_doc02_simsiam`): SimSiamModule.__doc__ contains 'CVPR 2021' and 'arXiv'
    - Test 11 (`test_doc02_barlow_twins`): BarlowTwinsModule.__doc__ contains 'ICML 2021' and 'arXiv'
    - Test 12 (`test_doc02_moco_v3`): MoCoV3Module.__doc__ contains 'ICCV 2021' and '2104.02057'
    - Test 13 (`test_doc02_dino`): DINOModule.__doc__ contains 'ICCV 2021' and '2104.14294'
    - Test 14 (`test_doc02_supcon`): SupConModule.__doc__ contains 'NeurIPS 2020', '2004.11362', and 'ClassBalancedSampler'
    - Test 15 (`test_doc02_supcon_finetune`): SupConFinetuneModule.__doc__ contains 'NeurIPS 2020', '2004.11362', and 'weight_decay=0.0'
    - Test 16 (`test_doc02_helper_rejects_missing_field`): _check_doc02 raises AssertionError when given a class with a docstring missing 'Paper:'
  </behavior>
  <action>
    Create `tests/test_docstrings.py` with the structure below. Mirror the test-file
    conventions of the existing test suite (`from __future__ import annotations`,
    section separator comment style).

    ```python
    """DOC-02 compliance tests — every LightningModule subclass class-level docstring
    must contain Paper, Authors, Venue, arXiv link, Gotchas, Reference implementation.

    These tests check the **class** docstring (cls.__doc__), NOT module docstrings.
    Per REQUIREMENTS.md DOC-02: 'Per-method docstring in each LightningModule subclass'.
    """
    from __future__ import annotations

    import pytest


    # ---------------------------------------------------------------------------
    # DOC-02 field checker
    # ---------------------------------------------------------------------------

    def _check_doc02(cls, venue_year: str, arxiv_fragment: str) -> None:
        """Assert class docstring contains all DOC-02 required fields."""
        doc = cls.__doc__
        assert doc is not None, f"{cls.__name__} must have a class docstring"
        assert "Paper:" in doc, f"{cls.__name__}: missing 'Paper:' field"
        assert "Authors:" in doc, f"{cls.__name__}: missing 'Authors:' field"
        assert "Venue:" in doc, f"{cls.__name__}: missing 'Venue:' field"
        assert "arXiv:" in doc, f"{cls.__name__}: missing 'arXiv:' field"
        assert venue_year in doc, (
            f"{cls.__name__}: docstring missing venue/year '{venue_year}'"
        )
        assert arxiv_fragment in doc, (
            f"{cls.__name__}: docstring missing arXiv fragment '{arxiv_fragment}'"
        )
        assert "Gotcha" in doc or "gotcha" in doc.lower(), (
            f"{cls.__name__}: missing 'Gotchas:' section"
        )
        assert "Reference implementation" in doc, (
            f"{cls.__name__}: missing 'Reference implementation:' field"
        )


    # ---------------------------------------------------------------------------
    # Era 1: Proxy Tasks
    # ---------------------------------------------------------------------------

    def test_doc02_instance_discrimination():
        from methods.instance_discrimination.module import InstanceDiscriminationModule
        _check_doc02(InstanceDiscriminationModule, "CVPR 2018", "1805.01978")


    def test_doc02_invariant_spread():
        from methods.invariant_spread.module import InvariantSpreadModule
        _check_doc02(InvariantSpreadModule, "CVPR 2019", "arxiv.org")


    # ---------------------------------------------------------------------------
    # Era 2: In-Batch / Queue / Prototype
    # ---------------------------------------------------------------------------

    def test_doc02_simclr_v1():
        from methods.simclr.module import SimCLRv1Module
        _check_doc02(SimCLRv1Module, "ICML 2020", "2002.05709")


    def test_doc02_simclr_v2():
        from methods.simclr.module import SimCLRv2Module
        _check_doc02(SimCLRv2Module, "NeurIPS 2020", "2006.10029")


    def test_doc02_moco_v1():
        from methods.moco.module import MoCoV1Module
        _check_doc02(MoCoV1Module, "CVPR 2020", "1911.05722")


    def test_doc02_moco_v2():
        from methods.moco.module import MoCoV2Module
        _check_doc02(MoCoV2Module, "2020", "2003.04297")


    def test_doc02_swav():
        from methods.swav.module import SwAVModule
        # SwAVModule class docstring was a one-liner placeholder before Plan 10-02 Task 1.
        # Verify the placeholder text is gone.
        assert "see module-level docstring" not in (SwAVModule.__doc__ or "")
        _check_doc02(SwAVModule, "NeurIPS 2020", "2006.09882")


    def test_doc02_infomin():
        from methods.infomin.module import InfoMinModule
        _check_doc02(InfoMinModule, "NeurIPS 2020", "2005.10243")


    # ---------------------------------------------------------------------------
    # Era 3: No-Negative
    # ---------------------------------------------------------------------------

    def test_doc02_byol():
        from methods.byol.module import BYOLModule
        _check_doc02(BYOLModule, "NeurIPS 2020", "2006.07733")


    def test_doc02_simsiam():
        from methods.simsiam.module import SimSiamModule
        _check_doc02(SimSiamModule, "CVPR 2021", "2011.10566")


    def test_doc02_barlow_twins():
        from methods.barlow_twins.module import BarlowTwinsModule
        _check_doc02(BarlowTwinsModule, "ICML 2021", "2103.03230")


    # ---------------------------------------------------------------------------
    # Era 4: Transformer
    # ---------------------------------------------------------------------------

    def test_doc02_moco_v3():
        from methods.moco_v3.module import MoCoV3Module
        _check_doc02(MoCoV3Module, "ICCV 2021", "2104.02057")


    def test_doc02_dino():
        from methods.dino.module import DINOModule
        _check_doc02(DINOModule, "ICCV 2021", "2104.14294")


    # ---------------------------------------------------------------------------
    # Supervised Contrastive
    # ---------------------------------------------------------------------------

    def test_doc02_supcon():
        from methods.supcon.module import SupConModule
        _check_doc02(SupConModule, "NeurIPS 2020", "2004.11362")
        # SupCon-specific gotcha that REQUIREMENTS.md SUP-01 calls out:
        assert "ClassBalancedSampler" in SupConModule.__doc__


    def test_doc02_supcon_finetune():
        from methods.supcon.module import SupConFinetuneModule
        _check_doc02(SupConFinetuneModule, "NeurIPS 2020", "2004.11362")
        # SupConFinetune-specific gotcha — weight_decay=0.0 on linear head:
        assert "weight_decay=0.0" in SupConFinetuneModule.__doc__


    # ---------------------------------------------------------------------------
    # Helper unit test
    # ---------------------------------------------------------------------------

    def test_doc02_helper_rejects_missing_field():
        """_check_doc02 must raise AssertionError when a required field is absent."""
        class _BadDoc:
            """Some method.

            Authors: nobody
            Venue: ICML 2099
            arXiv: https://arxiv.org/abs/0000.00000
            Gotchas:
            - none
            Reference implementation: https://example.com
            """
        # Missing "Paper:" — should fail
        with pytest.raises(AssertionError, match="missing 'Paper:'"):
            _check_doc02(_BadDoc, "ICML 2099", "0000.00000")
    ```

    Implementation notes:
    - Imports are EXPLICIT per-test (no `import methods` at module level) — keeps each
      test independently importable and avoids registry side effects from
      `methods/__init__.py` running during test collection.
    - Use `arxiv.org` as the fragment for InvariantSpreadModule rather than a specific
      ID — InvariantSpreadModule's existing arXiv URL is the CVPR proceedings URL, not
      arxiv. Just check the field is present.
    - Tests do not mutate any state; no fixtures needed.
    - The `_BadDoc` class lives inside the test function so its docstring is namespaced
      and does not interfere with anything else.
  </action>
  <verify>
    <automated>pytest tests/test_docstrings.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_docstrings.py` exists: `test -f tests/test_docstrings.py`
    - Contains the helper `_check_doc02`: `grep -F "def _check_doc02" tests/test_docstrings.py`
    - Has exactly 16 test functions: `grep -c "^def test_" tests/test_docstrings.py` returns 16
    - Each test imports its class explicitly: `grep -c "from methods\." tests/test_docstrings.py` returns at least 14 (one per non-shared module path)
    - Does NOT include `import methods` at top-level: `! grep -E "^import methods$" tests/test_docstrings.py`
    - All tests pass: `pytest tests/test_docstrings.py -x -q` returns exit 0
    - No regressions: `pytest tests/ -q --tb=line` returns exit 0
    - Test file size > 3KB (substantive content): `[ $(wc -c &lt; tests/test_docstrings.py) -ge 3000 ]`
  </acceptance_criteria>
  <done>
    DOC-02 compliance is enforced programmatically. Any future class docstring regression
    (e.g., a developer accidentally deleting the Gotchas section) is caught by
    `pytest tests/test_docstrings.py`. The Wave 0 gap from 10-VALIDATION.md is closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code → docstrings | Documentation is read by humans via `help()` / IDE tooltips |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Information Disclosure | Author names / contact info in docstrings | accept | Author names are public from arXiv listings; no PII beyond what is already published |
| T-10-05 | Tampering | Outdated reference implementation URL | mitigate | Reference each method's official upstream repo (facebookresearch/, deepmind/, google-research/, etc.); check links during Plan 10-06 final review pass |
</threat_model>

<verification>
- `pytest tests/test_docstrings.py -x -q` passes (16 tests)
- `pytest tests/ -q` returns exit 0 (no regressions)
- All 15 LightningModule subclasses have DOC-02 compliant class docstrings (verified by the test)
- `python -c "from methods.swav.module import SwAVModule; help(SwAVModule)"` displays full DOC-02 fields
</verification>

<success_criteria>
- DOC-02 satisfied: every LightningModule subclass has a class-level docstring containing paper title, authors, venue, year, arXiv/DOI link, 2-sentence algorithm description, gotcha list, reference implementation URL
- Programmatic enforcement: `tests/test_docstrings.py` catches future regressions
- SwAVModule placeholder docstring is replaced with full content
- SupCon Gotchas (class-balanced sampler, sum-outside, weight_decay=0.0) are documented at the class level
- No behavior regressions in the 326 existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation-and-tutorial/10-02-SUMMARY.md`
</output>
