---
plan: "11-04"
wave: 1
depends_on: []
files_modified:
  - core/__init__.py
  - core/config.py
autonomous: true
requirements_addressed:
  - EXPORT-01
  - CLEAN-01

must_haves:
  truths:
    - "from core import PredictorHead, SupConLoss, MultiCropDataset, method_dispatcher executes without ImportError"
    - "All four symbols appear in core/__init__.__all__"
    - "config.py contains exactly one InfoMinConfig definition"
  artifacts:
    - path: "core/__init__.py"
      provides: "4 new re-exports following try/except ImportError pattern"
      contains: "PredictorHead"
    - path: "core/config.py"
      provides: "single InfoMinConfig definition (verified, no change needed)"
      contains: "class InfoMinConfig"
  key_links:
    - from: "core/__init__.py"
      to: "core.projection.PredictorHead"
      via: "try/except ImportError block"
      pattern: "from core.projection import.*PredictorHead"
    - from: "core/__init__.py"
      to: "core.losses.SupConLoss"
      via: "try/except ImportError block"
      pattern: "from core.losses import.*SupConLoss"
    - from: "core/__init__.py"
      to: "core.data.MultiCropDataset"
      via: "try/except ImportError block"
      pattern: "from core.data import.*MultiCropDataset"
    - from: "core/__init__.py"
      to: "core.dispatcher.method_dispatcher"
      via: "try/except ImportError block"
      pattern: "from core.dispatcher import.*method_dispatcher"
---

<objective>
Add 4 missing public exports to core/__init__.py following the existing try/except ImportError pattern, and verify (without code change) that config.py contains exactly one InfoMinConfig definition.

Purpose: External users and tutorial notebooks expecting `from core import PredictorHead` currently get ImportError. The exports are trivial additions that follow the established pattern already used for ProjectionHead, InfoNCELoss, and other symbols. CLEAN-01 is a verify-only task — the REQUIREMENTS.md states "dead code at lines 72–83" but the actual content at those lines is BarlowTwinsConfig, not a duplicate InfoMinConfig. The single InfoMinConfig definition at line 137 is the only one.

Output: Modified core/__init__.py with 4 new exports and updated __all__; no change to core/config.py (verified clean).
</objective>

<execution_context>
@/Users/yi-tingli/Documents/Projects/ml_topic_contrastive_learning/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yi-tingli/Documents/Projects/ml_topic_contrastive_learning/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-code-fix-export-cleanup/11-CONTEXT.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From core/__init__.py (current state — full file):
```python
# core package — public API re-exports
# Each import is wrapped in try/except so that parallel execution of
# individual plans does not cause ImportError if a sibling module is not yet created.

try:
    from core.base import BaseSSLModule
except ImportError:
    pass

try:
    from core.backbone import build_backbone
except ImportError:
    pass

try:
    from core.config import TrainConfig, EvalConfig, load_config
except ImportError:
    pass

try:
    from core.data import ContrastiveAugmentation, MultiViewTransform, SSLDataModule
except ImportError:
    pass

try:
    from core.ema import EMAUpdater
except ImportError:
    pass

try:
    from core.losses import InfoNCELoss
except ImportError:
    pass

try:
    from core.memory_bank import MemoryBank
except ImportError:
    pass

try:
    from core.queue import MomentumQueue
except ImportError:
    pass

try:
    from core.optimizers import LARS
except ImportError:
    pass

try:
    from core.projection import ProjectionHead
except ImportError:
    pass

__all__ = [
    "BaseSSLModule",
    "build_backbone",
    "TrainConfig",
    "EvalConfig",
    "load_config",
    "ContrastiveAugmentation",
    "MultiViewTransform",
    "SSLDataModule",
    "EMAUpdater",
    "InfoNCELoss",
    "LARS",
    "MemoryBank",
    "MomentumQueue",
    "ProjectionHead",
]
```

Symbols being added — confirmed locations:
- PredictorHead: core/projection.py (line 64) — same file as existing ProjectionHead export
- SupConLoss: core/losses.py (line 111) — same file as existing InfoNCELoss export
- MultiCropDataset: core/data.py (line 138) — same file as existing SSLDataModule export
- method_dispatcher: core/dispatcher.py (line 58) — new file, no existing export from this module

From core/config.py — InfoMinConfig count (grep to verify):
  Only one definition exists: class InfoMinConfig at line 137.
  Lines 72–83 contain BarlowTwinsConfig, not a duplicate InfoMinConfig.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 missing exports to core/__init__.py</name>
  <files>core/__init__.py</files>
  <read_first>
    - core/__init__.py (full file — see existing try/except pattern and __all__ list)
    - core/projection.py (line 64: confirm PredictorHead class definition)
    - core/losses.py (line 111: confirm SupConLoss class definition)
    - core/data.py (line 138: confirm MultiCropDataset class definition)
    - core/dispatcher.py (line 58: confirm method_dispatcher function definition)
  </read_first>
  <action>
Rewrite core/__init__.py with exactly the following content. The four additions are:
- Extend existing `from core.data import ...` block to add MultiCropDataset
- Extend existing `from core.losses import ...` block to add SupConLoss
- Extend existing `from core.projection import ...` block to add PredictorHead
- Add new try/except block for method_dispatcher from core.dispatcher
- Add all four names to __all__

The complete new file content (copy verbatim):

```python
# core package — public API re-exports
# Each import is wrapped in try/except so that parallel execution of
# individual plans does not cause ImportError if a sibling module is not
# yet created.

try:
    from core.base import BaseSSLModule
except ImportError:
    pass

try:
    from core.backbone import build_backbone
except ImportError:
    pass

try:
    from core.config import TrainConfig, EvalConfig, load_config
except ImportError:
    pass

try:
    from core.data import (
        ContrastiveAugmentation,
        MultiCropDataset,
        MultiViewTransform,
        SSLDataModule,
    )
except ImportError:
    pass

try:
    from core.dispatcher import method_dispatcher
except ImportError:
    pass

try:
    from core.ema import EMAUpdater
except ImportError:
    pass

try:
    from core.losses import InfoNCELoss, SupConLoss
except ImportError:
    pass

try:
    from core.memory_bank import MemoryBank
except ImportError:
    pass

try:
    from core.queue import MomentumQueue
except ImportError:
    pass

try:
    from core.optimizers import LARS
except ImportError:
    pass

try:
    from core.projection import PredictorHead, ProjectionHead
except ImportError:
    pass

__all__ = [
    "BaseSSLModule",
    "build_backbone",
    "TrainConfig",
    "EvalConfig",
    "load_config",
    "ContrastiveAugmentation",
    "MultiCropDataset",
    "MultiViewTransform",
    "SSLDataModule",
    "method_dispatcher",
    "EMAUpdater",
    "InfoNCELoss",
    "SupConLoss",
    "LARS",
    "MemoryBank",
    "MomentumQueue",
    "PredictorHead",
    "ProjectionHead",
]
```

Decision references: D-08 (follow try/except ImportError pattern, add all 4 to __all__).
  </action>
  <verify>
    <automated>python -c "from core import PredictorHead, SupConLoss, MultiCropDataset, method_dispatcher; print('all 4 imports ok')"</automated>
  </verify>
  <done>
    - `python -c "from core import PredictorHead, SupConLoss, MultiCropDataset, method_dispatcher; print('ok')"` exits 0.
    - `grep -v "^#" core/__init__.py | grep -c "PredictorHead"` returns 2 (import line + __all__ entry).
    - `grep -v "^#" core/__init__.py | grep -c "SupConLoss"` returns 2 (import line + __all__ entry).
    - `grep -v "^#" core/__init__.py | grep -c "MultiCropDataset"` returns 2 (import line + __all__ entry).
    - `grep -v "^#" core/__init__.py | grep -c "method_dispatcher"` returns 2 (import line + __all__ entry).
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify CLEAN-01 — confirm single InfoMinConfig definition in config.py</name>
  <files>core/config.py</files>
  <read_first>
    - core/config.py (full file — scan for all class InfoMinConfig occurrences)
  </read_first>
  <action>
Run the following verification grep and record the result:

```bash
grep -n "class InfoMinConfig" core/config.py
```

Expected output: exactly one line — `137:class InfoMinConfig(_StrictBase):`

If the output shows exactly one line at line 137: CLEAN-01 is done. No code change required. Note in the SUMMARY: "CLEAN-01 verified: exactly one InfoMinConfig definition found at line 137. No duplicate to remove. REQUIREMENTS.md reference to 'lines 72–83' appears to be stale — lines 72–83 contain BarlowTwinsConfig, not a duplicate InfoMinConfig."

If the output shows MORE than one line: there IS a duplicate. Remove the duplicate definition block (whichever occurrence comes first, keeping the one with the full docstring at line 137). Then re-run the grep to confirm only one remains.

Decision references: D-09 (verify single definition; mark done if already clean).
  </action>
  <verify>
    <automated>grep -c "class InfoMinConfig" core/config.py</automated>
  </verify>
  <done>
    `grep -c "class InfoMinConfig" core/config.py` returns exactly 1.
    No structural change to config.py required (unless a duplicate is found — see action).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core package import | Public API surface for downstream consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-1 | Information Disclosure | core.__all__ expansion | accept | Exports are intentionally public API; no sensitive internals are added to __all__ |
| T-11-04-2 | Tampering | try/except ImportError swallows errors | accept | Pattern is pre-existing and intentional for parallel plan safety; production use should import from specific modules |
</threat_model>

<verification>
Run after both tasks complete:

```bash
# 1. All 4 new exports importable
python -c "from core import PredictorHead, SupConLoss, MultiCropDataset, method_dispatcher; print('EXPORT-01: all 4 imports ok')"

# 2. All 4 in __all__
python -c "import core; missing = [s for s in ['PredictorHead','SupConLoss','MultiCropDataset','method_dispatcher'] if s not in core.__all__]; print('Missing from __all__:', missing) if missing else print('__all__ ok')"

# 3. CLEAN-01: single InfoMinConfig
grep -c "class InfoMinConfig" core/config.py
# Expected output: 1

# 4. Existing exports still work (regression check)
python -c "from core import ProjectionHead, InfoNCELoss, SSLDataModule, BaseSSLModule; print('existing exports ok')"
```

All checks must pass.
</verification>

<success_criteria>
- `from core import PredictorHead, SupConLoss, MultiCropDataset, method_dispatcher` executes without ImportError.
- All four names appear in `core.__all__`.
- Existing exports (ProjectionHead, InfoNCELoss, SSLDataModule, BaseSSLModule, etc.) still importable.
- `grep -c "class InfoMinConfig" core/config.py` returns 1.
- core/__init__.py follows the existing try/except ImportError: pass pattern for all new imports.
</success_criteria>

<output>
After completion, create `.planning/phases/11-code-fix-export-cleanup/11-04-SUMMARY.md`
</output>
